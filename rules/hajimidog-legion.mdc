# 军团模式（Legion Mode）—— L3 级动态组织架构 + 蜂群末端执行

**默认休眠（本工作区 / 本 Agent 会话）**：军团模式**不干预**蜂群模式的日常运行。仅当满足下文「激活条件」时，由统帅切入军团流程；**未激活时**，一切仍按 `.cursor/rules/hajimidog-4plus1-swarm.mdc`（以下简称「蜂群 `.mdc`」）执行，本文件**不增加**任何额外义务。

## 与蜂群模式的关系（共存、不替代、不收窄）

- **蜂群（L2）是军团（L3）的原子执行单元**：军团不修改、不覆盖、不收窄蜂群 `.mdc` 中任何条款（写码回合 **R3+R4a+R4b** 与蜂群编队时序闸门、R2 强制触发、R4 清单实测门禁、可验证分级、高风险落盘等底线**照旧**；用语以蜂群 `.mdc` 为准）。
- **层级关系**：L3（军团组织架构）→ L2（蜂群小队）→ L1（单人直执行）。
- **两套规则并行加载**：蜂群 `.mdc` 管 L2 及以下；本文件管 L3 层级编排与跨部门协同。冲突时以**蜂群 `.mdc` 底线**为准（因其覆盖写码、验收、安全等不可收窄条款）。

## 术语分立：满编蜂群 vs 军团架构满编（防误触发）

- **满编蜂群**：**仅**在 **蜂群 `.mdc`** 定义与触发——须 **R1、R2、R3、R4a、R4b** 各至少一次 Task 的五段链（含闸门与统帅实测）。**本文件不另释、不替代**该义务。  
- **军团架构满编**（同义：**满编军团** / **全编制军团**）：**仅**表示在**已激活军团**后，按阶段二至四 **展开最大合理的 EX / MG / OP 部门架构**（调研 → 可编辑方案 → 冻结三件套）。**不**因其字面「满编」而触发蜂群五段；叶 OP 若**另行**下达 **满编蜂群** 口令，则以蜂群 `.mdc` 为准。  
- **单独「满编」二字**：统帅须按蜂群 `.mdc` **一句追问**意向为 **满编蜂群** 还是 **军团架构满编**，**禁止**默认其一。  
- **军团三级指挥（推荐口令 · L3 指挥链）**：专指冻结架构下的 **EX → MG → OP** 逐级发包与汇流。**不是**蜂群 **R1–R4 四角色**。用户只说「三级」「三层」而未带 **军团 / EX·MG·OP / L3** 锚点时，统帅须**一句澄清**指向 **L3 指挥链** 还是 **蜂群对抗角色**。

---

## 激活条件（何时进入军团模式）

满足**任一**即可激活：

1. **用户显式请求**：消息含「军团」「军团模式」「L3」「Legion」「多部门」「组织架构」「兵团」等可映射本规则语义的表述。
2. **统帅规模裁量为 L 级**：经 S/M/L 分级（见下文）判定为 L，且用户未降级。
3. **用户显式要求军团三级指挥（L3 指挥链）**：消息含可映射 **EX / MG / OP** 分层编排的表述，且意图为**组织级**调度（非仅梗概比喻），例如（非穷尽）：
   - 「**军团三级指挥**」「**L3 指挥链**」「**EX·MG·OP 链**」（**首选**，与蜂群 R1–R4 无歧义）  
   - 「**EX**」「**MG**」「**OP**」「**TIER=EX|MG|OP**」「高管层/中层/末端（**军团语境**）」  
   - 「逐级指挥」「分层指挥」「中间层发包」等**且**可判断为军团 **I/O / 军令状 / DAG** 语境（非蜂群对抗口语）时，等同于 L3 指挥链；**含混的**「三级指挥」「真隔离三级」单独出现时，统帅须按上文「术语分立」**一句澄清**再是否进入本条。  
   - 「**中间层发包**」「**《下级 Task 发包》**」「**仅向直属下级派发**」「**不得越级**（且锚定冻结架构）」「**逐级主题汇流**」「**逐级汇流块**」  
   **排除**：仅在比喻中随口说「像公司三层」、或未冻结架构就要求「来三个 agent 分层」但**不**接受军令状/DAG/I/O —— 统帅须一句澄清是否进入**本文件**全流程，或降级为单蜂群 + 留痕。

**退出**：用户明示「退出军团 / 不用 L3 / 降回蜂群」；或统帅判定当前阶段已收敛到单蜂群可承接，可建议降级并经用户确认。

---

## 军团六阶段强制门禁（用户收紧 · 与 S/M/L 关系）

- **触发**：满足上文「激活条件」**任一**、军团模式**已激活**的本条任务链（同 **CHAIN**）。
- **强制**：自激活起至 **阶段六** 终态汇流与回声校验结论交付前，统帅 **必须** 依次落实 **阶段一 → 阶段二 → 阶段三 → 阶段四 → 阶段五 → 阶段六** 的产出与门禁，**禁止**跳过任一阶段、以「合并阶段」名义省略应有交付物、或在**未完成前序阶段门禁**时进入后序阶段。
- **禁止**以 **S 级**、任务体量小、时间紧、或「先跑执行」等为由 **豁免** 六阶段中任一阶段；**S 级** 在**已激活军团**时仅表示 **冻结架构可极简**（如单层 OP、少节点），**不**表示可省略阶段二～六。
- **与多波次专节**：`WAVE≥2` 的再基线、Gap、结构化四字段豁免等 **不推翻** 本条对「每一波次须含完整阶段二～六闭环（含阶段五执行与阶段六汇流/回声）」的底线；**禁止**无 **WAVE-CLOSE-*** 等效四字段而口头跳过阶段二～四（与下文「多波次」专节一致取严）。
- **例外**：用户当轮消息 **明示** `退出军团` / `豁免军团阶段` / `军团阶段压缩`（非穷尽）及**具体豁免哪一阶段或哪几阶段**，且统帅在答复 **首段** 声明 **豁免范围、RISK_OWNER、剩余风险归属** 时，方可按声明压缩；**禁止**统帅自行口头压缩。

---

## S/M/L 规模分级（统帅首轮必判）

收到用户需求后，统帅在**首轮回复**中给出规模判定并告知用户（用户可升/降级）：

| 等级 | 判定依据 | 启动架构 | 典型示例 |
|------|---------|---------|---------|
| **S（小）** | 单领域、目标清晰、改动范围小 | **未激活军团**：**L1** — 统帅直接执行，不启动蜂群。**已激活军团**：仍须 **完整六阶段**（见上节）；架构可极简 | 修改按钮颜色；补一段注释 |
| **M（中）** | 单领域但有对抗价值，需多视角验证 | **未激活军团**：**L2** — 启动一支 4+1 蜂群小队。**已激活军团**：六阶段 + 通常多节点 | 重构鉴权模块；新增 API 端点 |
| **L（大）** | 跨多个专业领域、需多部门协同、系统级工程 | **L3** — 启动军团模式（六阶段强制） | 从零设计一款 App；企业级系统改造 |

**禁止**：对 S/M 级任务**在未获用户军团激活意图前**强行启动军团（浪费）；对明显 L 级任务只开单蜂群而不告知用户可升级。**已激活军团**时 **禁止**用 S 级跳过 **六阶段强制门禁**。  
- **与蜂群「统帅实现轨外置」叠加**：上表「**L1** — 统帅直接执行，不启动蜂群」指**不强制**多角色 Task 编排；**不**收窄蜂群 `.mdc` **写码回合**与 **统帅实现轨外置**——一旦当轮落入写码回合或用户显式蜂群分工，**默认**主体实现仍由 **R3 Task** 落地（用户明示 **`主帅直接改仓`/`统帅可落地代码`** 或蜂群豁免口令者除外）。叶 OP 与蜂群末端对齐见蜂群 `.mdc` 专条。

---

## 军团模式全流程（六阶段）

### 阶段一：领域识别与目标收敛

**门禁**：军团模式已激活时 **不可省略**；产出为下阶段输入，须在答复或落盘可指针化。

统帅分析用户需求，提取：
- **所属领域**（如：教育科技、游戏开发、企业 SaaS、医疗健康等）
- **核心目标一句话**
- **已知硬约束**（平台、预算、合规、工期等）
- **S/M/L 规模判定**（若为 L，进入阶段二）

### 阶段二：联网调研组织架构

**门禁**：军团模式已激活时 **不可省略**（用户明示豁免军团阶段且首段声明者除外）。

**强制**：统帅启动 **联网搜索（WebSearch）**，调研该领域**真实的专业级团队组织架构**。

调研目标：
- 该领域的**头部公司 / 成熟团队**通常如何分部门？
- 各部门的核心职责与交付物是什么？
- 部门之间的协作关系与信息流向？

**产出**：一份**领域组织架构参照报告**（含来源标注 A/B/C），作为下一阶段设计的输入。

### 阶段三：输出架构方案图（用户可编辑）

统帅基于调研结果，在回复中输出**组织架构方案图**，包含：

1. **层级结构图**（文本/树形/表格均可，清晰呈现层级关系）
2. **每个角色的名称、职责说明、预期产出**
3. **末端角色标注**（哪些角色将各带领一支蜂群小队）
4. **部门间信息流向**

**用户编辑权**：统帅**明确邀请**用户审阅并调整：
- 哪些角色需要**增加**？
- 哪些角色需要**删减**？
- 哪些角色需要**合并或拆分**？
- 层级关系是否需要调整？

**禁止**：未经用户确认就直接启动全部 Sub-agent。

### 阶段四：冻结架构 + 生成军令状 + 信息流调度

用户确认架构后：

1. **冻结组织架构**：架构图成为本任务链的**形状**，适用蜂群 `.mdc` 的形状保持规则——后续不得静默修改层级或角色。
2. **生成军令状（Mission Brief）**：见下文「军令状机制」。
3. **生成角色依赖图（DAG）**：从架构图的信息流向中提取角色间依赖关系，执行拓扑排序推导执行阶段（EP-0, EP-1, …）。若检测到循环依赖，**阻塞冻结**并请用户调整或指定打破点。
4. **生成角色 I/O 契约表**：为每个角色定义输入来源+格式、输出产出+格式、下游消费者。须与 DAG 邻接表**一致性校验**通过后方可冻结。
5. **落盘**（推荐）：架构图、军令状、DAG + I/O 契约表分别写入 `.cursor/swarm/legion/` 目录。

**冻结三件套**：架构图 + 军令状 + 调度方案（DAG + I/O 表）同时冻结，后续修改任一须用户明示且统帅声明推翻了哪些冻结条。

### 阶段五：分阶段执行（DAG 驱动 + Phase Gate）

**执行循环**（EP = Execution Phase，与军团全流程六阶段区分）：

```
EP-0：启动无依赖角色的 Task（并行，≤4 并发）
  ↓ Gate EP-0→EP-1（统帅执行门禁检查）
EP-1：启动仅依赖 EP-0 的角色 Task（统帅按 I/O 契约路由上游产出）
  ↓ Gate EP-1→EP-2
  …
EP-N：最后一批角色
  ↓ 进入阶段六
```

**角色实例化规则**：

- **军团架构角色 Sub-agent 全强制（用户收紧）**：冻结组织架构中**每一个 L3 角色节点**（不论层级、不论 S/M/L 标签）在执行期 **必须** 各对应 **至少一次** 独立 **Task Sub-agent**；**禁止**用主对话分节扮演代替未降级、未豁免的架构节点。**军团统帅（+1）** 仍驻主对话总线，**不要求**为统帅单独开 Task。**例外**：用户当轮 **明示** 免 Sub-agent / 诚实边界下客观无法 Task（首段声明，L2 蜂群底线仍按蜂群 `.mdc`）。
- 组织架构中**每一个角色**（不论层级）**必须**启动独立的 **Task Sub-agent**（与上条同义叠加，取严）。
- **末端角色**（架构最底层）：每个末端角色的 Sub-agent 作为其蜂群小队的**统帅（+1）**，再按蜂群 `.mdc` 的规则在 Task 内部调度蜂群角色（写码回合须 **R1、R2、R3、R4a、R4b** 与时序闸门；非写码链按蜂群最小充分裁量）。
- **带下级 L3 角色**（典型 **EX / MG**；若用户冻结架构中存在**非叶的 OP→OP** 等特例，同规则适用）：接收上级任务（来自军团统帅或其直接上级），按分任务拆解后**仅向其在冻结架构中的直接下级**发起 **Task**（《下级 Task 发包》）；汇流**仅直连下级**产出后，携带「逐级主题汇流块」向上级回报。**叶 OP** 的**唯一** L3 以下委派对象为**其蜂群小队（L2）**，仍按蜂群 `.mdc`。**禁止越过冻结架构中的必选中间层**向更下层直接塞满量上下文（`统帅直达 OP` 仅当架构已冻结为无对应中间层 / 用户明示且统帅留痕）。
- **并行策略**：同 EP 内角色并行启动；跨 EP 须经 Gate 放行。

**能力统一、上下文分化**：

- **技能（Skills）**：每个 Sub-agent 均可调用本框架下**全部** Skills，**不做限制**。
- **上下文（Context）**：每个 Sub-agent **仅接收**与其角色职责相关的最小充分信息 + 军令状（穿透）+ I/O 契约路由的上游产出。
- **知识侧重**：由 Task 正文中的角色描述与任务说明决定该 Sub-agent 的关注重点。

**Gate 门禁**：每个 EP 结束后、下一个 EP 开始前，统帅执行门禁检查（详见「信息流调度协议」章节）。Gate 连续 BLOCK 2 次后须向用户汇报。

**I/O 路由**：统帅在 Gate 通过后，依据 I/O 契约表将上游产出裁剪/打包到下游 Task 正文的「本层追加区 > I/O 路由输入」中。若下游 Task 由**中间层起草**，则「I/O 路由输入」以中间层发包中的裁剪结果为准，统帅仅做契约对齐、预算与违禁审查。

### 阶段六：汇流与回声校验

所有 EP 完成且所有 Gate 通过后：
1. **OP / 蜂群统帅**：将末端产出与 **《逐级主题汇流块》** 回报**直接上级**（MG 或统帅）
2. **中间层**：按汇流义务整合直连下级后，携 **《逐级主题汇流块》** 回报军团统帅
3. 军团统帅执行**回声校验**（见下文），并抽查 **ROLLUP** 落盘与锚点对账——Gate 是局部阶段检查，回声校验是全局终态检查，两者互补不替代
4. 通过校验后，军团统帅向用户输出最终汇流结论

---

## 军令状机制（Mission Brief）—— 信息穿透的核心

### 定义

军令状是一份**不可篡改的穿透文档**，在架构冻结时由军团统帅生成。**冻结组织架构中的每一个 L3 角色节点**（EX / MG / OP，含叶 OP）各自的 **Task Sub-agent** 正文都**必须携带**军令状全文或落盘路径。**蜂群（L2）内的 R1–R4 Task**（写码链须含 **R4a/R4b** 与时序闸门）不是架构表中的独立军团角色；其军令状相关义务按技能 `19`（OP→蜂群至少 ②③⑤ + **`[PENETRATING]` 原文全链**）与蜂群 `.mdc` 执行，并与叶 OP Task 穿透区（全文或路径）衔接。

### 军令状内容（五要素）

| 要素 | 说明 | 可否修改 |
|------|------|---------|
| ① 原始需求 | 用户原话（不改写、不摘要） | 禁止 |
| ② 冻结目标句 | 统帅提炼，用户已确认 | 仅用户可推翻 |
| ③ 硬约束 | 安全底线、不可变需求、技术限制 | 仅用户可推翻 |
| ④ 非目标 | 明确不做什么 | 仅用户可追加 |
| ⑤ 成功标准 | 可验证的验收口径 | 仅用户可修改 |

### 穿透规则

- **L3（军团架构角色）**：冻结架构图中**每一个角色节点**对应的 Task 正文**必须包含**军令状**全文或落盘路径**（与上文「定义」一致；**叶 OP** 的 L3 Task 同样适用）。
- **L2（蜂群 R1–R4）**：不按本条强求整份军令状内联；须满足 `.cursor/skills/hajimi-dog/03_AI行为准则/19_军团模式军令状与信息穿透.md` §2.1（**至少** ②③⑤）与 §2.5（**`[PENETRATING]`** 原文贯穿），并由**蜂群统帅（叶 OP 的 Sub-agent 内 +1）**按蜂群 `.mdc` 裁量组装各 R* Task。叶 OP 的穿透区仍须承载全文或路径，供 Read 衔接。
- **中间层角色**可为下级**追加**本层领域的补充上下文，但**不得删改**军令状中的任何一条。
- 若任何 Sub-agent 发现其任务与军令状存在矛盾，**必须上报**给其上级（最终到军团统帅），**不得自行解释绕过**。

### 落盘

军令状推荐落盘至 `.cursor/swarm/legion/MISSION-BRIEF.md`，供所有 Sub-agent Read 访问。

### 军令状修订与穿透区正文的纪律（防 +1 单方篡改）

- **穿透区**（军令状五要素全文、`[PENETRATING]` 原文）在已冻结后，**禁止**由军团统帅（+1）**单独**改写、弱化或「便宜合并」语义；修订须满足 **用户明示**（原话或可指针对象），或 **统帅起草修订草案 + 用户确认**（双留痕：用户确认指针 + 统帅摘要），并在答复/落盘**首段或专节**声明**废止或替代哪一条**冻结形状（与蜂群 `.mdc` **形状保持**对齐）。
- **Sub-agent 无权**修改军令状落盘正文；发现矛盾只能 **上报**（见下文 **G/L/O**）。

### 事实核对矛盾 · 多路升级（G-全局 / L-局部 / O-操作）

**控制面**：任何 **R2 / R4b / L3** 角色**不得**自行「中断整条军团」或修改穿透区；须在产出中写明 **矛盾条目、证据等级（A/B/C）、建议分流 G|L|O**，由 **军团统帅（+1）** 执行 **BLOCK、重派、升级用户**。本节约束与蜂群 `.mdc` **形状保持**、**Phase Gate**、**R4b 与统帅证据权威序**叠加适用。

| 代号 | 名称 | 触发（满足其一即可归类） | 统帅动作（摘要） |
|------|------|--------------------------|------------------|
| **G** | **全局** | 与军令状 **②③④⑤** 或 **`[PENETRATING]`** 冻结语义**无法在不修订契约下**调和；或触及 **合规 / 安全** 且继续会扩大风险 | **BLOCK** 后续 EP 与全局收敛宣告；**停止向下游传递**；按上节取得**用户确认（或草案+确认）**后 **重冻结** 军令状（及若受影响的架构/DAG），以 **新 WAVE/ROUND** **重启**军团 DAG。**禁止**频繁滥用 G：非上述触及面则默认 **L** 或 **O**（见 R1 撕裂与技能 `19` 模板）。 |
| **L** | **局部** | 矛盾限于 **直接上级任务书、I/O 裁剪、追加区** 与 **R2 / R4b / +1 可复核证据** 之间，**未**触及军令状与 `[PENETRATING]` 的冻结语义 | **BLOCK 当前 EP**；**上级任务书重做** 或 **统帅转译后重发**；**仅重发受影响下级**（含重挂蜂群 L2）。事实缺口须 **补 R2 Task** 或 **+1 复测** 后再下发。 |
| **O** | **操作** | 契约未变，仅 **实现 / 测试 / 非语义文案** 与 TC 不符 | 末端 **R3** 修补 → **+1 重跑 TC** → **R4b delta**；**不**重启军团。**禁止**借 O 修改 **`.mdc` / 军令状 / 角色权限 / A·B·C 定义**；若 diff 触及上述任一，须 **升级为 L 或 G** 并记入 `decisions_log` / `.cursor/swarm/RECENT-DECISIONS.md`。 |

**L3 与 R2 型事实分工**：**EX / MG** **不得**将无 **R2 Task 关门留痕**或 **+1 粘贴之命令/官方 URL+条款** 支撑的外部行为、厂商结论、可达性**保证**标为 **A 级**；此类陈述默认 **B/C**，并标 **待 R2 / 待统帅复测**。**A 级**工程事实须能指回 **+1 实测留痕**；**不得以 R4b 自然语言单独顶替**（与蜂群 `.mdc` 一致）。

**`decisions_log`（推荐）**：军团层新增矛盾处置条目时**建议**标注 `SCOPE=G|L|O` 与 `AFFECTS_SHAPE=yes|no`；若 `AFFECTS_SHAPE=yes` 而未获用户明示推翻与统帅留痕对齐形状，则 **Phase Gate 不得签发与矛盾相矛盾的 PASS**（可与 Gate **G3 无未解决矛盾** 合并理解）。

---

## 上下文摘要契约（Context Summary Contract）—— 多层结构化传递

### 摘要衰减规则（防 context 膨胀）

多层传递中穿透区始终不变，但每增一层会累积上游摘要。引入**摘要衰减**与**总量预算**：

| 规则 | 说明 |
|------|------|
| **穿透区** | 军令状全文或落盘路径，任何层级不可改，**不计入预算** |
| **直接上级追加区** | 全文传递（≤300 字） |
| **隔代上级追加区** | 压缩为摘要（每层 ≤150 字） |
| **总非穿透预算** | 全部上游摘要链 + 本层追加区 **≤800 字**（软上限，超过须统帅审查裁剪） |

### 各传递路径的 Task 正文结构

**统帅 → EX**：穿透区 + 战略指令区（≤300 字）+ 禁区

**EX → MG（经统帅路由或 EX 发包 + 统帅代发）**：穿透区 + EX 战略摘要（≤150 字；若由统帅从 EX 产出裁剪则标注「摘要者=统帅」）+ 本层任务书（≤300 字）+ 禁区 +（若由 EX 起草下级包）**《下级 Task 发包》**全文或指针

**MG → OP（经统帅路由或 MG 发包 + 统帅代发）**：穿透区 + EX 战略摘要（≤100 字，二次衰减）+ MG 执行计划摘要（≤200 字）+ 本层任务书（≤300 字）+ I/O 路由输入 + 蜂群指令 + 禁区 +（若由 MG 起草）**《下级 Task 发包》**全文或指针

**统帅直达 OP（降级或两层架构时）**：穿透区 + 统帅直发任务书（≤300 字）+ I/O 路由输入 + 蜂群指令 + 禁区

### 追加标注义务

每层追加的内容**必须**包含元数据：`追加者=<角色名> 追加层级=<EX|MG|OP> 追加轮次=ROUND-<N>`

### 追加内容冲突裁决序

军令状五要素（最高）> `[PENETRATING]` 标签 > EX 战略约束 > MG 执行约束 > OP 本层补充（最低）。任何层级发现下级追加与上级约束冲突时，须**上报**而非自行裁决。

### 禁区（全层级强制）

- 不得将**同层级他部门**的结论塞入本支任务书，除非经**军团统帅**按 I/O 契约裁剪转发
- 不得用推断替代军令状中的硬约束
- 不得将本层理解混入穿透区军令状正文
- 不得将上游摘要链中「隔代摘要」回充为「全文」（只减不增）

### Context 膨胀防控

- **摘要衰减**：隔代自动压缩
- **总量预算**：非穿透区总计 ≤800 字，超出须统帅裁剪
- **落盘指针替代**：长内容用路径引用代替内联
- **统帅审查**：每次 Gate 时顺带检查下游 Task 正文是否超预算

**禁止**：自由发挥式传递；把完整聊天历史一锅端塞给下级；跳过摘要衰减直接复制上游全文。

---

## 回声校验（Echo Check）—— 产出反向核对

### 触发时机

军团统帅在**最终汇流前**（所有末端蜂群产出已回传到军团层）执行回声校验。

### 校验内容

对照军令状五要素，逐条检查：

| 校验项 | 检查问题 |
|--------|---------|
| 目标对齐 | 末端产出是否仍在冻结目标的范围内？ |
| 约束遵守 | 硬约束是否在执行中被无视或变通？ |
| 非目标边界 | 是否有产出越界做了明确「不做」的内容？ |
| 成功标准 | 产出是否可按成功标准验收？ |
| 一致性 | 不同部门的产出之间是否存在矛盾？ |

### 回声与蜂群 TC 双向锚定（TC-ID）

- **目的**：避免「回声叙事」与蜂群 **TC-B / TC-E 冻结表**漂移；凡在回声或军团终态汇流中宣称 **P0 已满足 / 可验收 / 已对齐军令状⑤**，须能 **grep 到对应 `TC-xxx` 编号**（或显式写「待补 TC-ID + 责任方 + 时限」，否则不得标 **A 级**）。
- **推荐**：`ECHO-CHECK.md` 表增 **`TC-ID` 列**；军令状要素⑤「成功标准」在冻结时即分配或引用 **TC-B-*** / **TC-E-***（见技能 `20` 模板）。
- **与蜂群**：末端叶 OP 内蜂群统帅按蜂群 `.mdc` 冻结 **R4a** 表；回声行 **优先引用该表编号**，禁止两套独立叙述无互指。

### 校验结果（与 Phase Gate 裁决用语对齐）

- **`PASS`**：军令状五要素与跨部门一致性均对齐，且 **P0 级 TC**（若已冻结）均有 **A 级**证据或符合下文「用户接受」条款；军团统帅可输出最终汇流结论。
- **`BLOCK`（回声失真）**：存在未解决的偏离、硬约束违反或关键矛盾；须指明部门/角色与军令状条目，要求**重新执行**或**升级裁决**；**禁止**在未处理 `BLOCK` 项时宣布全局收敛。
- **`COND_PASS`**：语义与蜂群 `.mdc`「R4 清单执行门禁」中 **`PASS` / `COND_PASS` / `FAIL`** 一致（约 **166–169 行**：在 **全部 P0 实测通过** 前提下，存在仅人工说明的 **P1** 或已文档化的剩余风险）。军团层宣告 `COND_PASS` 时须**同步满足**蜂群侧对 `COND_PASS` 的定义，并在留痕中登记 **COND_ID、条件说明、负责消化方、消化时限**（与 Phase Gate 专节一致）。
- **用户明示接受（审美/体验等）**：若结论依赖主观满意，须落盘 **`ACCEPT` 记录**（接受项编号、日期、放弃的 **TC-ID** 或明确范围），并与回声行互指；**禁止**仅凭口头「满意」将结论标为 **A 级**。

### 渐进军令状收口与众人拾柴闸口（衔接 `PORTING`）

凡启用 **渐进军令状闭环**（`legion/PORTING-PROMPT-PROGRESSIVE-CHARTER-LOOP.zh-CN.md`）的用户任务链：**波次收口**落盘 `WAVE-CLOSE-*` 前，统帅须完成 **§4.2 F**（`SC` 列表或 **N=0**）并与 `skill-candidates/INDEX.md` **对账**；若存在 **`READY_FOR_REVIEW`**，须已执行或留痕 **技能 `21` §6** 用户提问。**再启下一波**前须通过该文件 **§5** 检查表（含 F、§6 两项）。上述条件不满足时，**U6** 为真，不得以「已收敛」掩盖渐进闭环缺口。

---

## 军团统帅职责（主对话 = 总线）

军团统帅是军团模式下的**最高编排者**，留在主对话，职责包括：

1. **规模裁量**：S/M/L 判定
2. **领域调研**：联网搜索组织架构参照
3. **架构设计**：输出方案图、收集用户反馈、冻结架构
4. **军令状生成**：冻结五要素
5. **DAG 生成与校验**：架构冻结时输出角色依赖图 + I/O 契约表，执行一致性校验与循环检测
6. **任务分发与代发**：① 按 DAG 拓扑序启动**无依赖或依赖已满足**的角色 Task（携带军令状 + I/O 路由输入）；② **代发**：收到中间层提交的**《下级 Task 发包》**（须含下级首行锚点草案、穿透区/军令状指针、`PARENT_REF=本中间层锚点`、禁区声明）后，核对 DAG + I/O 表，**在下一可用动作内**代为创建 Sub-agent Task；仅可做格式、预算、`[PENETRATING]` 合规审查，**不得篡改**军令状五要素与中间层已定稿的任务书语义。若中间层**声称已自行创建**下级 Task，以平台事实为准；**不得**将未创建标为 A 级已派发。
7. **Gate 门禁执行**：每个 EP 结束后执行门禁检查，按裁决放行或阻塞
8. **跨部门协调**：当部门间产出矛盾时，裁决或要求重做
9. **回声校验**：最终汇流前逐条比对军令状
10. **验收裁决**：若涉及写码落地，仍须执行蜂群 `.mdc` 的 **R4a 冻结表 + 统帅实测 + R4b 关门核对** 与 **PASS / COND_PASS / FAIL** 裁决（不得用「R3+R4」省略 **R4a/R4b**）
11. **众人拾柴（技能候选）**：对**可验收**的 Sub-agent/末端蜂群产出，若存在**可复用、有利于军令状收敛**的套路，按 `.cursor/skills/hajimi-dog/03_AI行为准则/21_军团众人拾柴与技能候选沉淀.md` 记入 `.cursor/swarm/legion/skill-candidates/`（`SC-NNN`）；在**波次收口**（见 `legion/PORTING-PROMPT-PROGRESSIVE-CHARTER-LOOP.zh-CN.md` 收口包）或**目标阶段性/最终收敛**时，**须向用户**说明各候选的**阶段、子任务、效果**并询问是否**升格**入正式 Skills（走技能 `02` + `skills_linter.py`）。**禁止**未经用户表态擅自写入正式 `SKILL.md` 树。

**与蜂群统帅的关系**：军团统帅 ≠ 蜂群统帅。军团统帅负责 L3 全局编排；各末端蜂群小队有自己的 +1 统帅负责 L2 内部对抗。信息经军团统帅裁剪后传递给蜂群统帅，蜂群统帅的汇流产出经中间层回传给军团统帅。

**双层门禁关系**：军团 Phase Gate（L3）管「跨角色集成质量——产出是否完备、可被下游消费」；蜂群 **R4 门禁（L2）**管「角色内部质量——写码是否通过黑盒清单、实测与关门核对」。两层独立运行，互不替代。

- **蜂群 `FAIL`**：对应叶 OP 产出不达标，军团 **不得**在更高层宣告全局 **PASS** 或「任务完成」；须 **BLOCK** 或触发重做/收敛循环（见下节「收敛与轮次熔断」）。
- **蜂群 `COND_PASS`**：合法存在时，军团 Phase Gate **不得**仅因「非 PASS」字样而误杀；须核对蜂群 `.mdc` 对 `COND_PASS` 的条件及留痕中 **COND_ID / 消费者 / 时限** 是否完备。
- **术语**：本文件 Phase Gate 与技能 `20` 统一以 **`COND_PASS`** 为规范词；历史 **`CONDITIONAL_PASS`** 与 **`COND_PASS` 同义**（附条件放行），新写入须优先用 **`COND_PASS`**。

### 众人拾柴（技能候选）与 Skills 体系

- **沉淀粉目录**：`.cursor/swarm/legion/skill-candidates/`（`SC-NNN` 候选、`INDEX.md`、模板）。  
- **操作说明**：`.cursor/skills/hajimi-dog/03_AI行为准则/21_军团众人拾柴与技能候选沉淀.md`。  
- **升格为正式 Skill**：须遵守 `02_技能体系维护与进化.md`，更新 `00_全局索引`，并运行 `python .cursor/skills/hajimi-dog/skills_linter.py`；**候选 ≠ 正式技能**，直至用户裁决。

---

## 军团角色三层级定义（EX / MG / OP）

L3 军团内部进一步划分为**三个子层级**，由 Task 首行锚点的 `TIER` 字段标识。层级代号**固定**，不得随意改名或混用。

### 层级总览

| 子层级 | 代号 | 典型角色 | 职责范围 | 是否带蜂群 | 产出形态 |
|--------|------|---------|---------|-----------|---------|
| **L3-高管层** | **EX** | CTO / CPO / CMO / CSO | 战略决策 + 任务拆解 + 跨域协调 | 否（默认） | 纲要 + 任务书 |
| **L3-中层管理** | **MG** | 工程总监 / 业务总监 / 运营总监 | 承上启下 + 多角色协调 + 汇流 | 否（默认） | 执行计划 + 任务书 |
| **L3-末端执行** | **OP** | 产品负责人 / 架构师 / 开发负责人 | 具体执行 + 带蜂群 | **是** | 方案 / 代码 / 文档 |

**带蜂群的例外**：用户明示或统帅判定某 EX/MG 角色须带蜂群时，可升格；须在架构冻结时标注，且该角色额外承担蜂群统帅义务（适用蜂群 `.mdc` 写码回合底线）。

**架构不强制塞满 EX/MG/OP**：若任务只需两层（如 EX 直辖 OP、无 MG），或只需扁平架构（全部 OP），统帅按 S/M/L 裁量即可，不得强制插入不需要的中间层。

### 同子层级多节点（平级编组 · EX/MG 再分包）

- **允许**：在**冻结架构图**中，同一 `TIER`（**EX** 或 **MG** 或 **OP**）下设置**多个**平级角色节点（示例：`TIER=EX` 的「战略」与「合规」；`TIER=MG` 的「产品中台」「数据中台」），将原由单一 EX/MG 承担的任务书**拆解**为多份《下级 Task 发包》，**各节点独立 Sub-agent**，首行锚点 `ROLE` 互不相同、`TIER` 相同。  
- **依赖**：平级节点之间若有输入输出顺序，**必须**画进 **DAG**（有向无环）；**禁止**依赖图上无边却默认「某平级已先产出」。收口方式须在 I/O 表写明（例：两 EX 均向**军团统帅**回报，或由冻结的某一 **MG** 节点仅作汇流收口再上报）。  
- **纪律不变**：仍**仅向冻结架构中的直连下级**发包与汇流；`带下级 L3 角色` 与 `不得越级` 条款对**每一个**有下级的节点分别适用。  
- **与「单 EX / 单 MG」图示的关系**：教学示意图常为**单线** EX→MG→OP；**实战可冻结为宽图**（同层多节点），二者不冲突，以阶段四冻结为准。

### 带下级 L3 角色（EX / MG / 架构许可的非叶 OP）分发义务

1. **接收**：从上级（军团统帅或上一级 L3 角色）获取穿透区 + 上游摘要 + 本层任务书。
2. **拆解**：将本层任务书拆解为**可独立分发**的**每一份《下级 Task 发包》**，每份须包含：军令状**全文或落盘路径**（不可改）+ 上游摘要链（仅追加本层摘要，不删改上游）+ 下级角色任务书 + 与 I/O 契约一致的输出格式说明 + 首行锚点草案（含 `PARENT_REF=<本中间层 Sub-agent 锚点>`、`TIER` 为下级对应 EX|MG|OP）+ 禁区声明 + 追加元数据「追加者=<本角色名> 追加层级=<EX|MG>」。
3. **下发路径（规范义务）**：
   - **首选**：中间层在宿主平台**具备**创建 Sub-agent Task 能力时，**由其亲自创建**直属下级 Task，并在回报中列出已创建锚点。
   - **降级（诚实边界）**：若仅军团统帅会话具备 Task 工具，中间层**不得**声称已完成下发；须输出完整《下级 Task 发包》并明确要求 **军团统帅代发**；统帅代发后中间层在后续汇流中引用**实际首行锚点**。
4. **禁止**：跳过冻结架构内的直接下级**越级**下发（例如架构为 EX→MG→OP 时，EX 不得直接对 OP 发包，除非架构冻结已去除 MG 并经用户确认）；私自删改军令状或 `[PENETRATING]` 原文。

### 带下级 L3 角色汇流义务（逐级主题汇流）

1. **收集**：接收**全部直接下级**的产出（由下级 Sub-agent 返回至中间层会话路径，或经统帅汇总回灌——以平台为准；中间层须在「逐级主题汇流块」中注明证据来源指针）。
2. **检查**：核验下级产出是否与本层《下级 Task 发包》及 I/O 契约对齐（格式、完备性、`decisions_log`）。
3. **摘要上报**：向上级提交 **《逐级主题汇流块》**（见下文专节）+ **汇流摘要**（各直连下级状态 + 关键结论合并 + 矛盾与风险 + `decisions_log` 合并）；**仅汇总直连边界**，不得冒充末端蜂群内部未经 OP 收口的全量细节。
4. **禁止**：汇流时篡改下级产出事实；隐瞒直连下级之间的矛盾；未完成直连下级到齐即标 **齐**。

### 逐级主题汇流块（全 L3 角色强制）

每个 EX / MG / OP 在向上级交付时（OP 向 MG 或统帅），须在产出中显式包含以下结构（可用 Markdown 小节或表格）：

| 字段 | 说明 |
|------|------|
| **本层职责一句** | 本轮在军令状下的边界与交付责任 |
| **直连下级清单** | 冻结架构中的直接汇报方及其 Task 状态（未发 / 已发 / 运行中 / 已归） |
| **下级到齐状态** | `齐` / `缺_<角色>` / `COND`（附条件；须能指向 **COND_ID** 或等价留痕，禁止空壳） |
| **主题摘要** | 仅压缩**直连下级**要点；建议 ≤200 字（超出用落盘指针） |
| **矛盾与风险** | 直连层级已暴露矛盾、待上级裁决项 |
| **上送指针** | 上级产出或 `ROLLUP` 落盘相对路径；无则 `NONE` |

**推荐落盘**（便于审计）：`.cursor/swarm/legion/rollups/ROLLUP-<CHAIN>-<TIER>-<角色slug>.md`（虚拟演练可用 `.cursor/swarm/legion/rollup-verify-demo/rollups/`）。

**OP 特例**：直连下级为**蜂群 R1–R4** 时，「直连下级清单」填蜂群角色及内部状态；**主题摘要**须为蜂群统帅汇总后的**单行树**或指针，不得把 R1–R4 原文不打折塞给 MG。

### 小组信息空间（Group Space）—— 领导辖内共识区

**定义**：以**某一 L3 领导角色为根**的子树，在执行期共享的**组内信息空间**；用于术语、过程共识、对内 checklist 等。**默认不穿透**到兄弟部门子树；**不等同**于军令状或全局已拍板事实库。

**路径（推荐）**：`.cursor/swarm/legion/group-space/<CHAIN>/W<波次>/<TIER>-<领导角色slug>/CONSENSUS.md`（可增拆文件；权威说明见同目录 `README.md`）。

**边界**：

- **军令状五要素** 与 **`[PENETRATING]` 原文** 优先于组空间任意内容；**禁止**在组空间改写或弱化之。
- **禁止**以组空间内容**替代**《下级 Task 发包》的最小信息集或《逐级主题汇流块》**必填字段**；可在汇流块「上送指针」中**附加**组空间路径。
- **对外**：兄弟组、军团统帅**不得**将组空间全文默认当作 **A 级**事实；须经 **I/O 裁剪**、Gate 或回声对账后方可进入跨组上下文。
- **生命周期**：本 **WAVE** 内读写；波次收口时**只读或归档**；**新 CHAIN** 或架构 BREAKING 变更须**新开路径**（禁止静默复用旧 `CHAIN` 冒充同一契约）。
- **叶 OP**：若仅辖 **L2 蜂群**，可由 **OP 作为蜂群 +1** 维护**蜂群组内**共识子页（仍用上述路径模式，`TIER=OP`）。

### 带下级 L3 角色与 Phase Gate

- **EX 层 Gate**：EX 产出完成后、向直属下级分发前执行。检查：纲要是否覆盖军令状全部目标；任务书拆解是否完备；跨域协调点是否已标注；EX 追加的战略约束是否与军令状兼容。
- **MG 层 Gate**：MG 产出完成后、向直属下级分发前执行。检查：执行计划是否与上游纲要对齐；下级任务书是否可执行且 I/O 契约完备；MG 追加的执行约束与上游兼容。
- **非叶 OP Gate**（仅当架构冻结含 **OP→OP** 等）：上一跳 OP 产出完成后、向直属下级 OP 分发前执行——检查同 MG 逻辑，且**不得**跳过中间层规范除非架构已冻结无该层。
- **叶 OP 层 Gate**：蜂群产出完成后、汇流至上级 L3 前执行（与现有 EP 间 Gate 合并）。检查：产出是否符合 I/O 契约；蜂群验收是否为 **`PASS`** 或合法 **`COND_PASS`**（须可核对蜂群 `.mdc` 定义及 **COND_ID** 留痕）；**`FAIL` 不得放行**。

### 各层级 decisions_log

每个 EX / MG / OP 角色的输出**均须**包含 `decisions_log`（已决策 + 排除项 + 理由 + 决策源角色）。MG 汇流时须合并下级 decisions_log 并去重，标注决策源。

---

## 信息流调度协议（DAG + Phase Gate + I/O 契约）

本章定义军团模式在阶段四冻结时生成、阶段五执行时驱动的三项调度机制。操作模板与细则见配套技能（`18` / `19` / `20`）。**本节不修改、不覆盖、不收窄蜂群 `.mdc` 中任何条款。**

### 角色依赖图（DAG）

- **生成时机**：阶段四，架构冻结后紧接生成。
- **格式**：邻接表（Markdown 表格），每行一个角色，列出其依赖上游与执行阶段（EP-N）。
- **拓扑排序**：入度为 0 的角色 = EP-0；仅依赖 EP-0 的 = EP-1；以此类推。
- **循环依赖**：检测到则**阻塞冻结**，须用户指定打破点或合并角色。打破的边记为 `[DEFERRED]`，降级为下游的非阻塞软条件。
- **EP 驱动执行**：统帅按 EP 顺序启动 Task，同 EP 内并行，跨 EP 须经 Gate。

### Phase Gate（阶段门禁）

- **位置**：每两个相邻 EP 之间。
- **四项检查**：① EP-N 全部角色已返回；② 产出通过 I/O 契约校验；③ 无未解决矛盾；④ 军令状穿透完整。
- **裁决**：**PASS**（放行）/ **BLOCK**（阻塞）/ **COND_PASS**（附条件放行）。**`CONDITIONAL_PASS`** 与 **`COND_PASS` 同义**，新写入统一用 **`COND_PASS`**。
- **COND_PASS 留痕（强制）**：宣告 **COND_PASS** 时须登记 **COND_ID**（如 `COND-001`）、**条件说明**、**负责消化方**、**消化时限**、**关联 TC-ID**（若已冻结）；语义须与蜂群 `.mdc`「R4 清单执行门禁」中 **`PASS` / `COND_PASS` / `FAIL`** 一致，**禁止**空壳附条件。
- **失败处理梯度**：补充 → 重做 → 降级 → 用户裁决。
- **最大重试**：同一 Gate 连续 BLOCK 2 次后须向用户汇报，提供选项：用户介入 / 降级角色 / 回退架构。
- **与回声校验的关系**：Gate = 局部（每个 EP 间，检查产出完备性与格式）；回声校验 = 全局（所有 EP 完成后，检查目标对齐与跨部门一致性）。两者互补，**不可互相替代**。
- **放行前文件安检（强制）**：签发 **PASS** 或 **COND_PASS**、进入下一 EP 前，军团统帅须已 **Read** 本链 `CHAIN-STATE.md` **§A**（精简下限区）及 **军令状** 中与本轮 Gate 相关的节（若有 **`[PENETRATING]`**，须逐字对齐该节原文，**禁止**仅靠对话或 IDE Summarize 梗概）。微型 Gate（同 EP 分批批间）至少核对 **§A** + 军令状要点指针（可写小节标题）。与技能 **`22` §12** 一致。
- **记忆接力口令（跨指蜂群 `.mdc`）**：用户当轮使用蜂群规则 **「记忆接力」** 专节所列**同义口令**时，军团统帅须在**本轮任何派单或签 Gate 之前**先按该专节完成 **Read 序**；完成后仍须满足本条「放行前文件安检」。移植提示词：`.cursor/swarm/记忆接力提示词.md`（`PORTING-PROMPT-MEMORY-RELAY.zh-CN.md` 为兼容入口）。

### 收敛、轮次熔断与 A 级核验（L3 与叶 OP L2）

- **收敛（工作可停止的必要条件）**：须满足其一且留痕可 **grep / 对账**：**(1)** 军令状⑤「成功标准」对应的 **P0 级 TC**（`TC-B-*` / `TC-E-*`）全部以 **A 级**证据闭环；**(2)** 蜂群统帅已裁决 **`PASS`**，或 **`COND_PASS`** 且 **COND_ID**、消费者、时限等按蜂群 `.mdc` 与上文 Phase Gate 专节完备；**(3)** 主观标准（审美、文风、体验等）须有 **`ACCEPT` 记录**（日期、范围、互指 **TC-ID** 或明示放弃的验收项），**禁止**仅凭对话「满意」标 **A 级**。
- **未收敛**：须继续修改与复验；**禁止**未满足上条即宣布任务链或军团链「完成」。
- **叶 OP 熔断（防 R3/R4 死循环）**：在同一 **CHAIN**、契约未推翻前提下，针对**同一交付目标**连续 **5** 次完成蜂群 **R3→R4b** 闭环仍**未**满足「收敛」，叶 OP 内蜂群统帅**须强制**重入 **R1 与 R2**（按蜂群 `.mdc` **编队时序闸门**：`R1+R2` 关门后再 `R3`…），补充边界与可核验事实后再进入下一轮 **`R3+R4a+R4b`**；**禁止**无新事实的同构重复。
- **军团层熔断**：在同一 **CHAIN** 下，全局**回声校验**连续 **5** 次仍未得到 **`PASS`** 或合法 **`COND_PASS`**（仍为 **`BLOCK`** 或等价未收敛），军团统帅**须**启动**思路重开**：至少包含 **R1 型**（边界/威胁 Task）与 **R2 型**（事实核对 Task）各一（Sub-agent 隔离），或经用户同意调整军令状/DAG/I/O 后重跑；**禁止**在无新冻结条前提下无限重复同级汇流敷衍。

### 角色 I/O 契约

- **生成时机**：阶段四，与 DAG 同时。
- **设计原则**：**消费者驱动**——每个角色的输入格式由其**下游角色**的需求定义，上游角色须保证产出满足所有下游期望。
- **格式**：I/O 契约表（Markdown 表格），每行一个角色，列出输入来源+格式、输出产出+格式、下游消费者。输出格式不得为空。
- **decisions_log 必传**：每个角色的输出中须包含 `decisions_log`（已决策事项 + 排除项 + 理由），防止跨 EP 的决策语境丢失。
- **一致性约束**：I/O 表的依赖关系须与 DAG 邻接表一致；不一致时阻塞冻结。
- **路由职责**：Gate 通过后，统帅按 I/O 表将上游产出裁剪/打包到下游 Task 正文。
- **不匹配处理**：格式不符 → 统帅转译或要求重做；内容缺失 → 要求补充；语义冲突 → 升级裁决。

### 执行中变更协议

用户在执行中要求调整架构或依赖时，按变更影响分级处理：

| 变更级别 | 定义 | 处理 |
|---------|------|------|
| **ADDITIVE** | 新增角色/部门，不影响已完成 EP | 热插入到当前或后续 EP，不回溯 |
| **COMPATIBLE** | 修改未执行 EP 的契约/条件 | 重冻结受影响 EP 的门禁 + I/O，不回溯已完成 EP |
| **BREAKING** | 改变已完成 EP 的前提假设 | **阻塞当前执行**，向用户报告影响范围与回溯成本，由用户决定是否回溯 |

变更须记录「变更前 DAG → 变更后 DAG → 影响的 EP → 是否回溯」，适用形状保持纪律。

### 降级与阶段压缩

当 Task 并发/总量不足时，调度协议支持以下压缩：

| 压缩方式 | 条件 | Gate 处理 |
|---------|------|---------|
| **同 EP 分批** | 角色数 > 并发限制（≤4） | 批间加微型 Gate（仅检 G1+G2） |
| **EP 合并** | Task 总预算不足 | 合并为 Super-EP，结束后完整 Gate |
| **线性退化** | 仅 1-2 Task 可用 | 按拓扑序逐个执行，每步即检 |
| **诚实边界** | 无 Task | 统帅分节扮演，仍按 DAG 顺序、仍遵 I/O 格式 |

**不可跳过**：Gate 检查可简化但不可取消；I/O 格式可降精度但不可免校验。

### Task 预算表（架构冻结时输出）

统帅在冻结调度方案时，须同时输出 Task 预算估算：EP 数 × 角色数 × 蜂群深度 → 预估 Task 总数。超过阈值（建议 ≤ 20）时**强制提醒用户**并提供降级选项（蜂群深度可调、角色合并、分批执行等）。

---

## 军团规模裁量机制（最小充分部门集）

类似蜂群的「最小充分角色集」，军团亦不应无差别全编制展开：

- **架构精简原则**：用户需求能被 N 个部门覆盖，就不设 N+1 个。
- **合并裁量**：两个角色职责高度重叠时，统帅可建议合并并说明理由。
- **按需展开**：初始可启动核心部门，待阶段性产出后再决定是否展开辅助部门。
- **用户决定权**：最终精简/展开由用户拍板。

---

## 关键约束穿透标签（Penetrating Tags）

军令状中的某些约束特别关键（如安全底线、法规合规、数据隐私），统帅可标记为 **`[PENETRATING]`**：

- 标记了 `[PENETRATING]` 的约束，在每一层传递时**必须原文复制**，不得摘要或改写。
- 中间层的「本层追加区」中若与 `[PENETRATING]` 约束冲突，以 `[PENETRATING]` 为准，且必须上报矛盾。

---

## 落盘与持久化

### 军团专属目录

- `.cursor/swarm/legion/MISSION-BRIEF.md` — 军令状
- `.cursor/swarm/legion/ARCHITECTURE.md` — 冻结后的组织架构图
- `.cursor/swarm/legion/SCHEDULE.md` — DAG 邻接表 + I/O 契约表 + EP 执行计划 + Task 预算
- `.cursor/swarm/legion/ECHO-CHECK.md` — 回声校验记录（推荐列：**军令状要素 / 对齐结论 `PASS|BLOCK|COND_PASS` / `TC-ID` / `COND_ID` / `ACCEPT` 指针**；与叶 OP 内蜂群 **R4a 冻结表**互指）
- `.cursor/swarm/legion/rollups/` — **推荐**：各角色《逐级主题汇流块》落盘（可与任务链子目录并用）
- `.cursor/swarm/legion/group-space/` — **小组信息空间**根目录（见专节；每链每波按领导分子目录）
- `.cursor/swarm/legion/refs/` — **建议**：多波次 **阶段二** 增量/全量调研参照报告根目录（如 `RESEARCH-W<n>-<CHAIN>.md`）

### 与蜂群落盘的关系

- 军团落盘**不替代**蜂群 `.mdc` 要求的 `.cursor/swarm/RECENT-DECISIONS.md` 等落盘。
- 末端蜂群小队内部的验收、裁决仍按蜂群 `.mdc` 落盘。
- 军团层面的裁决（如跨部门冲突解决、架构变更）追加到军团目录。

### 多波次（WAVE）再启与 L3 Task 真隔离（默认强制）

**目的**：防止「第一波走满 EP+逐级发包」，**第二波**在未推翻冻结架构的前提下被默认压缩成主窗单线或仅重开叶 OP，导致 **L3 上下文串味** 与 **Gate/I/O 对账断裂**；并防止 **仅重跑阶段五** 却省略 **调研—架构—冻结** 再基线，导致 **Gap 与组织形状脱锚**。

- **术语**：**真隔离**指 **冻结架构中的每一个 L3 角色节点**在**本轮执行**中仍各对应 **至少一次独立 Sub-agent Task**（与是否恰好 EX→MG→OP **三层**无关；两层架构则两个节点各一 Task）。**禁止**用主对话同一上下文 **代替** 未降级架构中的必选中间层《下级 Task 发包》链。
- **与「三级」的关系**：规则**从不**保证第二波「一定和第一波一样是三层」。第一波若冻结为 EX→OP 或全 OP，第二波**沿用同一形状**时仍同构；**若**用户要**升级**为更深中间层，须回 **阶段三/四** 重新编辑架构并走 **BREAKING/ADDITIVE** 变更纪律，**不得**口头生效。

#### 第二波次起：阶段二～四再基线（默认强制）

**适用范围**：**`WAVE≥2`**（或渐进军令状下**下一波再启**、或 G 路径 **`新 WAVE`/`新 CHAIN`** 后）且**仍处于军团模式**、尚未用户明示 **退出军团**。

- **默认不得**「只跑阶段五」即宣称合规：**须**在阶段五之前完成本轮 **阶段二、三、四**（与上文军团模式「阶段二～四」定义一致；阶段编号不因波次而跳过）。
- **阶段二（联网调研 + 参照报告）**  
  - **允许轻量形态（增量调研）**：对准 `WAVE-CLOSE-*` 的 Gap 与上轮冻结差异补 **delta**，但**禁止**无外部检索的「凭记忆续写」。  
  - **硬条件**：至少 **一次 WebSearch**（或宿主等价、可留痕的外查）；产出须 **落盘** 为**更新版领域组织架构参照报告**（建议：`.cursor/swarm/legion/refs/RESEARCH-W<WAVE>-<CHAIN>.md` 或链目录下等价路径），含 **相对上波的增量问题清单** + **检索摘要或可引用指针**（A/B/C 分级照旧）。  
- **阶段三（可编辑架构）**  
  - **须**再次输出 **可编辑** 组织架构方案图（技能 `18` 口径），并 **邀请用户显式确认** 本轮形状（含「零编辑」）。  
  - **零架构变更** 时须留 **`NO_CHANGE=YES`** + **证据指针**（指回上波冻结 `ARCHITECTURE`/`SCHEDULE` 节标题 + 核对摘录或用户原话）；**禁止**仅靠聊天「没变」而无落痕。  
- **阶段四（冻结三件套）**  
  - **须**再次冻结 **架构图 + 军令状 + 调度方案（DAG + I/O）**；并对冻结物做 **可见 bump**（如 `## WAVE N 冻结区` / `VERSION=W<N>` / 变更摘要），标签 **`ADDITIVE` / `NO_CHANGE` / `BREAKING`** 三选一 + 一句理由。  
  - **禁止**静默覆盖首轮冻结区而不留 **diff、指针或版本戳**。  
- **与 `PARTIAL_RELIGHT` 的关系（正交）**：**`PARTIAL_RELIGHT=YES` 只约束阶段五「哪些 L3 节点须再派 Task」**；**不自动** 豁免阶段二～四。阶段二～四能否压缩仅由下文 **Gap 分级 + 结构化豁免** 决定。  
- **Gap 分级（须在 `WAVE-CLOSE-*` 或再启附件可 grep）**  
  - **`GAP_MATERIAL`**：触及 **② 目标句 / ③④ / 部门边界 / DAG 边 / I/O 契约 / `[PENETRATING]` 解读** 中任一项。→ **默认**阶段二～四 **走满形态**（阶段二不得仅以「一句增量」糊弄；须能支撑新冻结）；**不得**用 `PARTIAL_RELIGHT` 单独绕过。  
  - **`GAP_TRIVIAL`**：仅 **叶 OP 内执行细节、文案、非契约参数** 等，且 **不改变** 冻结架构与 I/O；Gap 矩阵「已有」列须有 **可复核指针**。→ 允许 **轻量再基线**：阶段二仍须 **WebSearch + 落盘**（可短小），阶段三须 **用户确认 + `NO_CHANGE` 路径**，阶段四须 **bump + 再冻结**。  
- **结构化豁免（压缩二～四的唯一契约入口）**：若主张本轮 **不执行** 某阶段或 **低于上述默认为「压缩」**，须在 **`WAVE-CLOSE-*`**（或再启前等效单）写明四字段（缺一则统帅 **不得**签发再启）：`EXEMPT`（如 `PHASE2`/`PHASE3`/`PHASE4`/`PHASE2-4`）、`SCOPE`、`EVIDENCE_PTR`、`RISK_OWNER`（`USER`/`HOST`/`COMMANDER`）。**禁止**口语豁免。  
- **统帅自检真值表（摘要）**：`PARTIAL_RELIGHT` 与 阶段二～四 **无充分必要关系**；**`GAP_MATERIAL` + `PARTIAL_RELIGHT=YES`** 须在收口包说明 **为何仍局部重开**（例：Gap 仅单支 OP 且 I/O 不变），否则 **不得**签发局部重开。在 **`GAP_TRIVIAL`** 下，**禁止**无四字段即默认整个 **二～四** 免跑（等效于 `EXEMPT=PHASE2-4`）；若用户明示承担且四字段齐全（含 **`RISK_OWNER=USER`**），方可压缩，且 **不得**将结论标为 **A 级**契约等价。

#### 第二波次起：阶段五 L3 Task 真隔离（与上节叠加）

- **默认**：当 **架构 / DAG / I/O 契约**未经用户明示 **BREAKING** 推翻时，须在 **完成本条上一节阶段二～四留痕** 后，**按本轮冻结架构** 执行 **阶段五**（EP 拓扑 + Phase Gate + 逐级发包/代发纪律），**每一 L3 节点**仍须 **独立 Task**；Task 首行锚点须带 **`WAVE=<递增>`** 及 **`CHAIN`/`ROUND`**（**禁止**静默复用旧子代理 transcript 冒充本轮关门）。  
- **局部重开（阶段五范围）**：仅当 **`WAVE-CLOSE-*`** 含 **`PARTIAL_RELIGHT=YES`** + **`GAP_TRIVIAL|GAP_MATERIAL`** + **子 DAG/单支 OP 范围** + **用户确认摘录**，且 **`SCHEDULE` diff** 对未纳入节点标 **`N/A` + 上波证据指针** 时，方可 **阶段五** 不派发未纳入节点 Task；并与上一节 **Gap/豁免** 正交自检，**禁止**矛盾。  
- **架构压缩（减层/扁平）**：一律 **BREAKING**，走「执行中变更协议」与 PORTING 形状保持专节；**禁止**因换波次默认减层。  
- **与渐进取口叠合**：PORTING **§5** 再启检查表须自检：**阶段二～四再基线**、**阶段五** **全树** 或 **合规 `PARTIAL_RELIGHT`**；`SCHEDULE` **diff** 须能看出本轮属哪一种。

---

## Task 正文强制首行锚点（与蜂群 `.mdc` 对齐扩展）

军团模式下的 Task 正文**第一行**须包含：

```
CHAIN=<任务链ID> ROUND=<递增整数> WAVE=<波次整数,多波次军团建议必填> ROLE=<角色名> DEPT=<部门名> LEVEL=L3|L2 TIER=EX|MG|OP|N/A PARENT_REF=<指针>
```

| 字段 | 说明 | 取值 |
|------|------|------|
| `CHAIN` | 任务链 ID | 自定义标识符 |
| `ROUND` | 同链内递增整数 | 1, 2, 3… |
| `WAVE` | **军团多波次**执行序号 | 1, 2, 3…；单波次可省略或恒为 1；**第二波次起 L3 Task 建议必填**（与「多波次再启与 L3 Task 真隔离」对齐） |
| `ROLE` | 角色名 | 如「CTO」「工程总监」「架构师」 |
| `DEPT` | 部门名 | 如「技术中心」「产品中心」 |
| `LEVEL` | 军团/蜂群大层级 | `L3` / `L2` |
| `TIER` | **L3 子层级**（新增） | `EX`（高管）/ `MG`（中层）/ `OP`（末端）/ `N/A`（L2 填此值） |
| `PARENT_REF` | 上级指针 | 上级 Task 锚点摘要或 `NONE` |

**组合约束**：`LEVEL=L3` 时 `TIER` 必须为 EX/MG/OP 之一；`LEVEL=L2` 时 `TIER` 必须为 `N/A`。

---

## 诚实边界

若当前环境**无法启动足够数量的 Task**（平台并发限制等）：

- 军团统帅须在答复**首段**说明限制，并声明 **诚实边界** 生效范围。
- **降级策略**：优先保留末端蜂群小队的 Task 隔离（L2 底线不可破）；L3 中间层角色**仅在此诚实边界下**可由军团统帅**分节扮演**（每节标题注明部门/角色名），且 **不得** 冒充已完成对应 Sub-agent 的锚点关门。**Task 可用时禁止**以此条规避 **L3 节点 Task 全强制**。
- **禁止**：以平台限制为由同时取消 L3 分节**和** L2 蜂群隔离；**禁止**在 Task 可用时用分节扮演代替**已冻结架构中的必选 L3 节点** Task。

---

## 触发词与话术路由（非穷尽）

| 用户说法 | 动作 |
|---------|------|
| 军团 / 军团模式 / 兵团 / Legion | 激活军团模式全流程，且 **强制** 走满 **阶段一至六**（见「军团六阶段强制门禁」） |
| L3 / 多部门 / 组织架构 | 同上 |
| **军团三级指挥** / **L3 指挥链** / **EX·MG·OP 链**（首选，防与蜂群四角色混读） | **等同**激活军团意图 + L3 子层级冻结导向；首轮须 S/M/L 裁量；进入阶段三至冻结三件套 |
| EX / MG / OP（军团锚点）/ Task 首行 `TIER` 取 EX·MG·OP / 高管层·中层·末端（军团表） | **等同**激活军团意图；冻结架构须标 `TIER`；进入阶段三至冻结三件套 |
| 逐级指挥 / 分层指挥（**且**军团 I/O·军令状语境清晰） | **等同**上几行 |
| **含混**单用「三级指挥」「真隔离三级」而无军团锚点 | **不**自动激活；统帅**一句澄清**是 L3 指挥链还是蜂群 R1–R4，再路由 |
| 中间层发包 / 下级 Task 发包 / 逐级主题汇流 / 逐级汇流块 / 不得越级下发 | 指向阶段五 **带下级 L3 角色分发义务** 与 **汇流义务**；未冻结架构则阻塞并回阶段三/四 |
| Phase Gate（军团 EP 间）/ DAG+I/O / 冻结三件套 | 指向阶段四与「信息流调度协议」；若用户只要「口头 Gate」须澄清是否落盘 I/O |
| 加角色 / 减角色 / 调整架构 | 返回阶段三让用户编辑 |
| 降回蜂群 / 不用军团 / 退出 L3 | 退出军团模式，仅保留蜂群 |
| **满编蜂群** / **蜂群满编** / **五段满编** | **不由本表处理**；见蜂群 `.mdc` **满编蜂群口令**（R1–R4a/R4b 五段） |
| **军团架构满编** / **全编制军团** / **满编军团**（**仅**指部门架构展开） | 在已激活军团下按调研结果展开**最大合理 EX·MG·OP**；**不**触发蜂群五段强制 |
| 精简军团 / 最小军团 | 只启动核心部门 |

---

## 规范栈与冲突裁决

- **权威源**：本文件为军团模式（L3）的强制条款权威源。
- **优先级**：蜂群 `.mdc`（L2 底线）> 本文件（L3 编排）> 配套技能（`18`/`19`/`20` 阐释与模板）。
- **不可收窄**：本文件**不得**收窄蜂群 `.mdc` 的任何底线条款。
- **用户优先**：用户对具体指令的明示（含豁免）优先于本文件默认条款。
- **配套技能**：
  - `18_军团模式组织架构动态生成.md` — 调研与架构设计操作细则
  - `19_军团模式军令状与信息穿透.md` — 军令状模板与穿透纪律
  - `20_军团模式回声校验与规模裁量.md` — 回声校验清单与 S/M/L 判定指南
