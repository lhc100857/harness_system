# 蜂群模式（4+1 · L2）

请在本仓库按 **4+1 蜂群模式** 处理本条及后续追问（除非我另行写明退出或豁免口令）。

## 必须遵守的权威源

- `.cursor/rules/hajimidog-4plus1-swarm.mdc`（编队时序闸门、写码回合 **R3+R4a+R4b**、R2 强制触发、R4 清单实测与 PASS/FAIL、豁免口令等）
- 与军团叠加时：`.cursor/rules/hajimidog-legion.mdc` 与之 **并集取严**（见 `AGENTS.md`）

## 起手与路由

- 命中强触发时按 `.mdc` 完成 **S1–S5** 最小 Read 集；总路由速查：`.cursor/skill.md` 第一节 → 按需下钻 `.cursor/skills/hajimi-dog/00_全局索引/SKILL.md` 与技能 **09 / 12 / 16 / 22** 等。

## 执行纪律（摘要）

- **统帅（+1）** 驻主对话：解析 → 分发 → 汇流 → 续聊任务状态；**R1/R2/R3/R4a/R4b** 在强制情形下须 **Task** 真隔离，禁止主对话分饰多角代替。
- **写码回合**（改源码、构建配置、影响协作的规则语义等）：**R1+R2 关门 → R3**；**R4a** 可与 R3 并行（契约级、不喂实现细节）；**R3 关门且证据回灌后 → R4b**；统帅按 R4a 表 **亲自实测** 再裁决 PASS/FAIL。
- 若我本轮需要 **主帅直接改仓**，我会另写 **「统帅可落地代码」** 或 **「主帅直接改仓」**；未写则默认 **统帅实现轨外置**（R3 Task 落笔）。

## 我的本轮诉求

（在此继续写具体任务、约束、CHAIN-ID、验收口径等。）
