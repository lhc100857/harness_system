# 军令状（Mission Brief）

**任务链 ID**：`LEGION-RUBRIC-DEMO-001`  
**生成时间**：2026-03-27  
**军团统帅**：主对话助手  

---

## ① 原始需求（用户原话，禁止改写）

> 用军团模式（L3）处理下面这件事：我们有一个内部 CLI 小工具，只做「把仓库里的 Markdown 导出为单文件 HTML」，现在希望增加可选暗色主题和可配置页脚版权行。  
> （中略：流程、落盘路径、约束、非目标、成功标准——见用户完整消息。）

---

## ② 冻结目标句（统帅提炼，用户已确认）

在**不改变现有 CLI 已约定的用户数据路径**前提下，为 Markdown→单文件 HTML 导出能力补齐 **可选暗色主题** 与 **可配置页脚版权行** 的**设计与验收闭环**（本轮以军团落盘与蜂群子交付为主，**不合并实现到主分支**）。

---

## ③ 硬约束（不可违反）

- `[PENETRATING]` **不得改变**现有 CLI 行为以外的**用户数据路径**（输入/输出根路径、默认产物位置语义保持不变；新增仅允许为**显式可选**的 CLI 标志/配置，不改变未带参时的路径契约）。
- `[PENETRATING]` **不引入新外部服务**（无网络依赖的新 SaaS、无新增运行时拉取的外部 API）。
- 冻结三件套落盘路径：`.cursor/swarm/legion/demos/LEGION-RUBRIC-DEMO-001/`（`ARCHITECTURE.md`、`MISSION-BRIEF.md`、`SCHEDULE.md`）。
- DAG **无环**；至少 **2 个 EP**；至少 **2 个叶 OP** 各留 **蜂群 R4 裁决留痕**。
- **本次不写进正式 hajimi-dog 技能树**；可复用套路仅入 `skill-candidates` 草稿并走技能 21 收口提问。

---

## ④ 非目标（明确不做）

- 不实现完整代码合并到主分支。  
- 不做性能压测。  
- 不把本演示链的叙述升格为正式 Skill（除非用户后续明确升格并走 `02` 维护流程）。

---

## ⑤ 成功标准（可验证）

- 冻结三件套路径可查且内容完整。  
- DAG 无环；EP ≥ 2（本链为 EP-0/1/2）。  
- 叶 OP ≥ 2，且**各自**有一次蜂群 **R4 裁决留痕**（见 `swarm-traces/`）。  
- `ECHO-CHECK.md` 与军令状五要素**逐条对齐**；跨部门一致性表无未解决矛盾。  
- 各 L3 角色产出（含演示用 EX/MG/OP 汇流）含 **`decisions_log`**（已决策 / 排除项 / 理由）。

---

🔒 本文档为军令状，任何层级 Sub-agent 不得修改。发现矛盾须上报军团统帅。
