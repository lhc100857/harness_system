# SC-001 — CLI 导出 HTML：主题 + 页脚 增量包（设计收口）

**候选 ID**：`SC-001`  
**状态**：`READY_FOR_REVIEW`  
**录入日期**：2026-03-27  
**录入者**：军团统帅（+1）

---

## 任务语境（用户裁决用 · 必填）

| 字段 | 内容 |
|------|------|
| **CHAIN** | `LEGION-RUBRIC-DEMO-001` |
| **WAVE / 波次** | EP-2（叶 OP 并行 + 蜂群） |
| **军团阶段** | 阶段五执行收口 / 阶段六回声前 |
| **角色 / 层级** | `OP-THEME` / `OP-FOOTER` + 蜂群 R1–R4 |
| **子任务一句话** | 为单文件 HTML 导出 CLI 收敛「可选暗色主题」与「可配置页脚」的契约、错误语义与验收表 |
| **军令状对齐** | 推进 ② 设计/验收闭环与 ⑤ 可验证 TC，而不触碰「不并主分支」非目标 |

---

## 可复用套路（候选技能核心）

- **触发条件**：内部 CLI 将 Markdown 转为**自包含单文件 HTML**，需增加**展示层**（主题）与**用户可控字符串**（页脚）且约束「无新外部服务、路径契约不变」。  
- **操作步骤**：  
  1. EX 冻结：默认 CLI 不变；新 flag 仅附加；禁止外链依赖。  
  2. MG 冻结：HTML 壳层插槽（`#export-footer`）与主题根钩子二选一（`html` 类或 `body data-theme`）。  
  3. OP-THEME：CSS 语义变量 + 暗色覆盖块；代码高亮与页面主题成对设计；R2 对齐 WCAG 对比度分层（1.4.3 / 1.4.1 / 1.4.11）。  
  4. OP-FOOTER：默认 **实体转义或 `textContent`**；`--footer` 与 `--copyright-file` **互斥 fast-fail**；分 `NOT_FOUND` / `READ` / `ENCODING` / `SOURCE_CONFLICT`。  
  5. R4：默认无参基线快照 + 暗色 flag + XSS 字符串三者为 P0；无实现时 **COND_PASS** 并声明重跑条件。  
- **产出物**：`MISSION-BRIEF` 级契约 + `TC-B-*`/`TC-E-*` 表 + `decisions_log` + R4 裁决一行。  
- **禁忌 / 反模式**：页脚默认 `innerHTML` 直灌；主题与页脚职责交叉改 DOM；未定义 CLI 冲突优先级。

---

## 验收与评分（统帅填）

| 维度 | 取值 | 说明 |
|------|------|------|
| **Gate / 蜂群** | COND_PASS | 与「无 runnable」一致 |
| **收敛贡献** | 高 | 可直接驱动下一波实现 |
| **可复用性** | 中 | 适用于静态导出/文档 CLI 同类增量 |
| **证据指针** | `.cursor/swarm/legion/demos/LEGION-RUBRIC-DEMO-001/`；OWASP XSS Cheat Sheet（R2 OP-FOOTER）；W3C Understanding 1.4.3（R2 OP-THEME） |

**综合建议**：`与现有技能 13/10/11 合并索引条目` 或 `仅作本仓 legion 笔记`；若升格，建议挂到 `03_AI行为准则` 下「内部工具 CLI 产物」专题而非独立长文。

---

## decisions_log

- **已决策**：页脚纯文本+转义为默认；双源互斥；主题单一钩子。  
- **排除项**：默认页脚 HTML；隐式覆盖优先级；新 CDN。  
- **理由**：XSS 与审计可追溯；与 EX/MG 穿透一致。

---

## 升格记录（若已合并到正式 Skill）

- **目标文件**：（待定）  
- **合并日期**：—  
- **用户确认**：否（待技能 21 提问）
