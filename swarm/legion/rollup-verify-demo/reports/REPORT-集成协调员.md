# 角色汇报 — 集成协调员（MG）

**CHAIN**：`DEMO-CHAIN-ROLLUP-001`  
**TIER**：MG  

## 本层职责（本轮）

把 EX 纲要落实为 **OP 可执行 Task 包**，并 **仅汇总 OP 的直连交付** 上报 EX，不篡改事实。

## 逐级汇流履行

- **直连下级**：末端实施员（OP）。  
- **发包**：`PACK-OP` 已通过统帅代发（演练等价）。  
- **回收**：`ROLLUP-OP` 已到齐（`COND`：蜂群虚拟）。  
- **主题摘要**：OP 交付为规则/技能补丁 + 演示目录；风险已登记「真实 env 须实测」。  

## 模式检验点

- **禁止越级**：未绕过 OP 直接写实现结论为「已完成实测」。  
- **汇流块**：`ROLLUP-MG` 字段完整，参见落盘文件。

## 指针

- `rollups/ROLLUP-DEMO-CHAIN-ROLLUP-001-MG-集成协调员.md`  
- `SCHEDULE.md`（EP-2 输入输出）
