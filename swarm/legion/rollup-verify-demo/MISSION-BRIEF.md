# 军令状（Mission Brief）— 虚拟演练

**任务链 ID**：`DEMO-CHAIN-ROLLUP-001`  
**生成时间**：2026-03-26  
**军团统帅**：主对话助手（演练口述）

---

## ① 原始需求（用户原话，禁止改写）

> 先帮我改动规则接受中间层分发 task 然后实现我的主题汇流然后实现完之后启动一个虚拟军团任务专门用来检验这种逐级汇流模式检验完之后把军团信息按每一个角色出一张 markdown 汇报

## ② 冻结目标句（演练版）

在仓库内落地规则与技能，使军团 **EX/MG 可对其直接下级起草 Task 发包**（平台限制时由统帅代发），并强制 **《逐级主题汇流块》**；本目录提供 **可 walkthrough 的虚拟军团** 与 **按角色汇报**。

## ③ 硬约束

- [PENETRATING] **不得收窄** `.cursor/rules/hajimidog-4plus1-swarm.mdc` 中写码回合、R4 实测门禁等底线。
- 中间层 **禁止** 越过冻结架构内应有的层级 **越级** 下发（本架构 EX→MG→OP 不可 EX 直连 OP）。
- 虚拟演练 **不写** 密钥、token、隐私请求体。

## ④ 非目标

- 不在本演练中修改蜂群 `.mdc` 正文。
- 不要求真实拉起 ≥4 个并发 Sub-agent；以 **`VERIFICATION-TRACE.md` 纸面等价**检验。

## ⑤ 成功标准（可验证）

- `.cursor/rules/hajimidog-legion.mdc` 含：**中间层发包**、**统帅代发**、**逐级主题汇流块**、**ROLLUP 路径**。
- 技能 `18`/`19`/`20` 与规则 **无互相否定**（分发、汇流、Gate GR*）。
- `rollup-verify-demo/` 含：架构、军令状、排期、TRACE、≥3 份 `rollups/`、**四份** `reports/REPORT-*.md`（含统帅）。

---

🔒 本文档为军令状（演练），Sub-agent 不得改五要素正文；矛盾上报军团统帅。
