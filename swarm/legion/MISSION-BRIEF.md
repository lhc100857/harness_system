# 军令状（Mission Brief）— 已冻结

**任务链 ID**：`LEGION-VIRT-LINGXI-001`  
**生成时间**：2026-03-25（统帅落盘）  
**军团统帅**：主对话助手  

**冻结声明**：用户回合 2 输入「继续」，统帅解释为：对 `CHAIN=LEGION-VIRT-LINGXI-001` 的架构草案与军令状草案予以推进，完成阶段四落盘；与下文矛盾时以用户后续明示为准（形状保持）。

---

## ① 原始需求（用户原话）

> 来一个超大型的任务作为虚拟任务然后以军团模式进行

---

## ② 冻结目标句

以**虚拟演习**方式，为「灵犀」多城市群应急指挥与资源调度平台」建立 **L3 军团**可执行编排：已冻结的组织架构可映射为**无环 DAG**，每角色具备 **I/O 契约**与 **decisions_log** 要求；末端 OP 角色默认挂载 **4+1 蜂群**。本链**不**包含真实编码交付与真实政务对接（除非你另开任务链并升级约束）。

---

## ③ 硬约束（不可违反）

- [PENETRATING] **隐私与数据**：任何产出（含虚拟数据集设计）须按「最小必要、分级脱敏、可审计访问」口径表述；禁止把可识别自然人轨迹、精确到人的敏感细节作为可交付示例默认值。
- [PENETRATING] **多租户/多机构隔离**：安全与数据策略须在架构层有**单一事实源**（策略 / 加密体系 / 审计链路可对齐），禁止只在某一端「口头保证」。
- **可验证分级**：对外可依赖结论须标 **A/B/C**；**B/C** 不得冒充 **A**。
- **Sub-agent 纪律**：EX/MG/OP 的 Task 须遵守 `.cursor/rules/hajimidog-legion.mdc` 与 `.cursor/rules/hajimidog-4plus1-swarm.mdc`；军令状五要素不得被下级改写。

---

## ④ 非目标

- 不申请或假设已取得任何实战资质、不连接真实政务内网。
- 本条链路**默认不**进入应用源码写码回合；若进入写码回合须另起契约并满足蜂群 **R3+R4** 与 **R4 实测门禁**（除非用户明示豁免）。

---

## ⑤ 成功标准（可验证）

- `SCHEDULE.md` 中 **DAG 无环**且 EP 可执行。
- **I/O 契约表**与 DAG 两侧可对账（输入来源 = 依赖上游；消费者覆盖下游边）。
- 每一角色产出模板均预留 **decisions_log**（已决策 / 排除项 / 理由）。
- 军团终局前可运行 **回声校验**（对齐 `ECHO-CHECK.md` 清单）。

---

**用户回合 2 补充（程序性）**：`继续` → 授权统帅生成并落盘 `.cursor/swarm/legion/{ARCHITECTURE,MISSION-BRIEF,SCHEDULE}.md`。

🔒 本文档为军令状，任何层级 Sub-agent 不得修改正文。发现矛盾须上报军团统帅。
