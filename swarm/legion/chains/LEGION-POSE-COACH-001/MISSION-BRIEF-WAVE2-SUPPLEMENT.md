# 军令状补充 — WAVE 2 — LEGION-POSE-COACH-001

**母军令状**（只读、不得改写正文）：`MISSION-BRIEF.md`  
**本文件**：W2 **增量冻结**；与母件冲突时以**用户明示**或**母件 [PENETRATING]** 为准。

---

## W2 冻结目标句（增量）

在 **不违反** 母军令状 ③④ 前提下，增加**开发期**能力：提供 **本机 HTTP 服务** 与小程序 **可选联调**，用于拉取 `poses_manifest` 等 JSON；**默认失败回退包内资源**，**不**将相机画面或默认可识别肖像上传至该服务器。

---

## W2 硬约束（增量）

- [PENETRATING] **延续母件**：任何网络传输仍须满足母件 ③；**开发服务器不得**作为「默认上传肖像改进产品」的通道。  
- **开发服务器范围**：仅响应 **只读配置类** API（如 manifest、health）；若扩展 POST，须 **显式文档化字段** 且 **不得** 要求上传原始相机帧作为联调前提。  
- **真机调试**：局域网 IP 须可配置；**不**把开发者内网地址写入上架版本默认配置。

---

## W2 非目标（增量）

- 不上线生产级账号体系、计费、CDN 策略（W2 仅限本地/开发联调）。  
- 不在 W2 强制引入云开发控制台配置。

---

## W2 成功标准（可验证）

- 在本机启动 `dev-server` 后，开发者工具在 **关闭域名校验**（或等价配置）下，`coach` 页可 **成功拉取** manifest；停服或断网时 **自动回退** 包内 `require` 数据，**不白屏**。  
- `GET /api/health` 返回可解析 JSON，**退出码/HTTP** 可供脚本验收。  
- `README` 含启动步骤与 **隐私/域名校验** 提醒。

---

🔒 W2 收口后由统帅更新 `ECHO-CHECK.md` 或 `ECHO-CHECK-W2.md` 与 `README`（链级）。
