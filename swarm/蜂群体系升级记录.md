# 蜂群 4+1 对抗体系 — 升级记录

**用途**：每次对 **规则（`.mdc`）**、**蜂群配套技能**、**便携移植包** 做**可感知升级**时，由维护者在本文**文末追加一节**（勿删改历史），便于团队与未来的自己快速知道「改了什么、为何值得升级」。

**建议写法**（每节）：日期 + 标题 + **升级特性（条列）** + **好处举例（1～3 条，可带场景）** + **关联路径**（可选）。

**与 `RECENT-DECISIONS.md` 的分工**：后者偏**当轮拍板与风险**；本文偏**产品化/体系化变更说明**与**用户可感知的收益**，可互相链同一批文件。

---

## 2026-03-29 — XWM（外置结构化工作记忆）命名冻结 + 通用移植提示词

**升级特性**

- 在技能 **`22_主帅上下文生命周期管理.md`** 增加 **§1.1 XWM**：全称 **外置结构化工作记忆**、推荐缩写 **XWM**、**禁用裸 `SSM`** 以免与 S/M/L、ML、云厂商缩写混淆；明确 **CHAIN-STATE** 与军令状的**权威序**。
- 新增 **`.cursor/swarm/PORTING-PROMPT-XWM.zh-CN.md`**：效力免责、蜂群/军团衔接指针、**可复制提示词块**（用户 / 统帅 / 外机降级说明）。
- **`00_全局索引/SKILL.md`**、**`swarm/README.md`**、教学稿 **`04-实现思路-…`** 互指上述文件，便于检索与移植。
- **（补录）进阶外置 XWM+**：技能 **22 §1.2** 与 **PORTING-PROMPT-XWM §0.5** 约定可选 **`L1-MIRROR.md`**、**`task-reflux/{ROUND}-{ROLE}.md`**、口头拍板**同轮**进 §B / `RECENT-DECISIONS`；与 `CHAIN-STATE` 冲突时仍以 **§A/§B** 与军令状为准。

**好处举例**

1. **场景**：要把「落盘扩上下文、对话保持干净」迁到外机——单文件携带术语 + 习惯 + 提示词，且文首声明**单独复制不触发本仓门禁**。
2. **场景**：协作者误写 SSM——文档层有统一替代表述与对照表。
3. **场景**：长对话或 IDE 压缩后——L1 草稿与 Task 回流有磁盘镜像，子代理不默认「记得上一轮桶内全文」。

**关联路径**：`.cursor/swarm/PORTING-PROMPT-XWM.zh-CN.md`，`.cursor/skills/hajimi-dog/03_AI行为准则/22_主帅上下文生命周期管理.md`，`.cursor/skills/hajimi-dog/00_全局索引/SKILL.md`，`docs/teaching/pro-ai-hajimi-governance/04-实现思路-Rules-Skills-Swarm-Task.md`

---

## 2026-03-23 — 便携包 v1.7.0：纳入技能 17（工具成功与 UI 不可见调试）

**升级特性**

- 便携包 **`03_AI行为准则`** 由 9 个增至 **10 个**：新增 **`17_工具成功与UI不可见-调试清单.md`**（工具调用“成功”但前端/工作区无感时的分层排查）。
- **`rebuild_swarm_pack.py`** / **`build-swarm-portable-pack.ps1`**：`SKILL_PREFIXES` / `$skillPrefixes` 增加 **`17`**，`PACK_VERSION` → **1.7.0**。
- **`00_INDEX` 便携索引**（`dist/swarm-pack-files/SKILL.md`）：增补 `17` 条目，frontmatter 描述与包版本对齐。
- **`MANIFEST.json`**、**`INSTALL.zh-CN.md`**：版本与技能计数同步。

**好处举例**

1. **场景**：对话里工具返回成功但页面不刷新、文件未出现——可按 `17` 分层区分「真执行失败 / 路径错仓 / 前端未订阅」等，减少盲改解析或重复跑同一工具。

**关联路径**：`dist/rebuild_swarm_pack.py`，`dist/build-swarm-portable-pack.ps1`，`dist/swarm-pack-files/SKILL.md`，`dist/蜂群体系的移植包/MANIFEST.json`，`dist/蜂群体系的移植包/INSTALL.zh-CN.md`，`.cursor/skills/hajimi-dog/03_AI行为准则/17_工具成功与UI不可见-调试清单.md`，`dist/swarm-4plus1-portable-v1.7.0.zip`

---

## 2025-03-21 — 便携移植包 v1.4.0（规则与技能全量同步）

**升级特性**

- 移植包技能由 6 个扩展为 **9 个**：新增 `10_对抗架构展陈`、`15_外部技能包桥接索引`、`16_蜂群触发路由与形状保持`。
- 增加 **`external-cursor-pack-bridge`**，外来 `cursor_skills` 包仅作桥接索引，不合并异工程正文。
- 规则文件与仓库 **`hajimidog-4plus1-swarm.mdc`** 同步（含形状保持、扩展触发语、R2·Breaking、规范栈 `16` 等）。
- 分发 zip 采用 **ASCII 文件名** `swarm-4plus1-portable-v1.4.0.zip`，减少部分环境下中文文件名乱码。
- `dist/rebuild_swarm_pack.py` / `build-swarm-portable-pack.ps1` 与 `INSTALL.zh-CN.md`、`MANIFEST.json` 对齐 v1.4.0。

**好处举例**

1. **场景**：要把蜂群体系迁到新仓库。v1.4.0 一次合并即可带上「展陈 HTML 实践 + 外来包对表 + 话术路由」，少跑几轮「缺文件再补拷」。
2. **场景**：协作者只熟悉旧版 6 技能包。升级记录明确 **多了哪三份技能、桥接是干什么的**，减少「我解压了但 Agent 找不到 16」的沟通成本。

**关联路径**：`dist/swarm-4plus1-portable-v1.4.0.zip`，`dist/蜂群体系的移植包/`，`dist/rebuild_swarm_pack.py`

---

## 2025-03-21 — 规则与技能：触发路由、形状保持、R2 加码

**升级特性**

- **`.mdc` frontmatter `description`** 增补触发相关关键词，便于 Cursor 规则侧路由识别。
- 新增 **「扩展触发语」**：子代理、别串台、P0/门禁、形状保持、同一任务链等；并写明 **命中 ≠ 自动满编**（分层触发）。
- 新增小节 **「形状保持与防目标漂变」**：禁止静默覆盖已冻结前提；用户当轮明示优先；禁止为省 Token 省略「续聊任务状态」。
- **R2 强制触发** 增加 **依赖主版本 / Breaking** 未核验时的默认命中条件。
- **规范栈** 纳入技能 **`16`**，**`15`/桥接** 标明为可选阐释。
- 新技能 **`16_蜂群触发路由与形状保持.md`**：分层触发表、话术→Read、极端情况、续聊模板；**`00_全局索引`**、`09`、`12` 交叉引用。
- **`RECENT-DECISIONS.md`** 已按「改 `.mdc` 语义须落盘」追加对应条目。

**好处举例**

1. **场景**：长对话里用户改口但没说清。形状保持要求 **显式点出矛盾并重冻结**，减少「模型默默按最新一句把前文契约吃掉」的漂移。
2. **场景**：用户说「满编」和只说「帮我验收 P0」混在一起。分层触发避免 **每句都开四轮 Task**，成本可控同时不误触总线纪律。
3. **场景**：升级了 major 依赖还没读 migration guide。R2·Breaking 条款提醒先 **事实核验再写码**，降低「能编译但行为静默变」的回归。

**关联路径**：`.cursor/rules/hajimidog-4plus1-swarm.mdc`，`.cursor/skills/hajimi-dog/03_AI行为准则/16_蜂群触发路由与形状保持.md`，`.cursor/swarm/RECENT-DECISIONS.md`

---

## 2026-03-22 — 任务链隔离、留痕三档、双轨验收与脚本工具链

**升级特性**

- **任务链（CHAIN）与父会话隔离**：不同冻结目标/交付物类型推荐**新父会话**；同会话多链须 **CHAIN + 轮次表** 留痕，防子代理目录「一桶杂烩」。
- **Task 首行锚点**：满编或显式 R1–R4 时 **第一行强制** `CHAIN` / `ROUND` / `ROLE` / `PARENT_REF`；**禁止**用子代理文件 mtime 冒充因果序。
- **留痕三档**：叙事稿 / 结构化全记录 / 字节镜像；与 **`16` 的 L0–L3 触发分层**明确**不同名**；冲突时用 **`OMIT_REASON`** 封闭枚举（技能 `12`/`14`/`08` 互指）。
- **统帅汇流骨架**：冻结事实 → 转化表 → 与用户旧结论矛盾 → 诚实边界 → 待决与指针（含 CHAIN/ROUND）。
- **R4 分轨**：**TC-B-**（业务/方案黑盒）与 **TC-E-**（工程/构建）分列 P0 最小集合；端到端集成缺陷须明确主责轨或 COND_PASS 条件。
- **工具链**：可重复操作**入仓脚本**；**禁止**关键路径依赖未验证的**单行 PowerShell 拼接**。
- **技能 `08`**：从纯历史标题扩展为「会话留痕与导出」专篇，并指针 `_export_sales_swarm_full_md.py` 示例。

**好处举例**

1. **场景**：销售方案与导出器迭代共用一会话，子代理文件数暴涨、复盘对不上轮次。CHAIN + 新开会话 + 轮次表让「哪条链产出了什么」可 grep。
2. **场景**：用户既要「自然语言全记录」又要审计。三档 + OMIT 把**人读**与**字节对账**拆清，减少「同一文件既要无省略又要无代码」的需求打架。
3. **场景**：方案写得漂亮但 CI 没跑。TC-B / TC-E 分轨提醒统帅**两轨都要有关闭条件**，降低字面上双过、实际上漏端到端的概率。

**关联路径**：`.cursor/rules/hajimidog-4plus1-swarm.mdc`，`.cursor/skills/hajimi-dog/03_AI行为准则/08_当前对话记录导出.md`，`12` / `14` / `16`，`00_全局索引/SKILL.md`，`./_export_sales_swarm_full_md.py`，`.cursor/swarm/RECENT-DECISIONS.md`

---

## 2026-03-22 — 用户身份与意图三字段（执行者 · 人设 · 下游用途）

**升级特性**

- `.mdc` 新增 **「用户身份与意图冻结」**：每条链至少冻结 **执行者身份（真实/假定）**、**期望扮演身份（人设）**、**产出下游用途**；与 **A/B/C**、形状保持、REQ、高风险、Task 最小充分信息衔接。
- **必须追问**：高风险/写码/改 `.mdc` 语义/对外用途等条件下，三字段缺失或与人设冲突时**先澄清或阻塞**；可推断时标 **B** 并请用户确认。
- **合规边界**：禁止无证照人设下的律师/医师/审计签字式结论；须降级为信息整理 + 建议咨询持证方。
- **统帅分发协议**：**解析**须提取三字段；**汇流骨架**以三字段为第 (1) 步；**续聊状态**须可看到三字段。
- **可选口令**：`强制身份开场`（及同义口令）→ 下一条助手回复**先**输出 **三问 + 快速选项**（`12` §3.2）；无口令则**不**对闲聊默认三问。
- 技能 **`12`/`14`/`16`/`08`/`00`**：模板、误读表、可验证说明、导出隐私、索引互指已同步。

**好处举例**

1. **场景**：同一对话里「写着玩」和「要进标书」混在一起。冻结**下游用途**后，断言强度与 TC 可与场景对齐，减少误把草案当对外承诺。
2. **场景**：用户希望助手当「行业专家」。**期望扮演身份**与**不得越权背书**并排，人设服务叙述但不突破底线。

**关联路径**：`.cursor/rules/hajimidog-4plus1-swarm.mdc`，`03_AI行为准则/12`、`14`、`16`、`08`，`00_全局索引/SKILL.md`，`.cursor/swarm/RECENT-DECISIONS.md`

---

## 2026-03-22 — 便携包 v1.6.0：同步主仓最新 + 路径泛化进包

**升级特性**

- 自 **v1.5.0** 起主仓已完成 **规则/技能/落盘** 的**相对仓库根路径**表述与 `12` §6.1 移植规范；本版 zip 由当前 `.cursor` **全量重建**，便携包内规则与技能与主仓一致。
- **`MANIFEST.json`** 版本 **1.6.0**；**`INSTALL.zh-CN.md`**、**`其他电脑-Cursor-使用说明.zh-CN.md`**（`dist/swarm-pack-files/` 源模板）中「当前构建版本」同步为 **v1.6.0**。
- **`rebuild_swarm_pack.py`**：`PACK_VERSION` → **1.6.0**，产物 **`swarm-4plus1-portable-v1.6.0.zip`**（构建时会删除旧版 `swarm-4plus1-portable-v*.zip`）。

**好处举例**

1. **场景**：换目录/换盘符克隆仓库后，解压本 zip 合并 `.cursor`，文档内不再依赖 `g:\…` 等本机绝对路径（与主仓 `RECENT-DECISIONS` 迁移记录一致）。

**关联路径**：`dist/rebuild_swarm_pack.py`，`dist/蜂群体系的移植包/MANIFEST.json`，`dist/swarm-pack-files/其他电脑-Cursor-使用说明.zh-CN.md`，`dist/swarm-4plus1-portable-v1.6.0.zip`，`.cursor/rules/hajimidog-4plus1-swarm.mdc`，`03_AI行为准则/12_蜂群规则与技能体系维护指南.md`

---

## 2026-03-22 — 便携包 v1.5.0：其他电脑 Cursor 使用说明

**升级特性**

- 新增 **`dist/swarm-pack-files/其他电脑-Cursor-使用说明.zh-CN.md`**，重建 zip 时自动复制到 **`蜂群体系的移植包/`** 根目录，与 `INSTALL.zh-CN.md` 并列。
- **`INSTALL.zh-CN.md`** 文首增加「换电脑/新同事」指针与表格一行；**`MANIFEST.json`** 版本 **1.5.0** 并登记新文件。
- **`rebuild_swarm_pack.py`**：`PACK_VERSION` → **1.5.0**，重建后产物 **`swarm-4plus1-portable-v1.5.0.zip`**。

**好处举例**

1. **场景**：同事在自己的笔记本上第一次解压 zip，不知道要先 Open Folder 到项目根、规则不生效怎么排查——可按专文逐步操作。

**关联路径**：`dist/swarm-pack-files/其他电脑-Cursor-使用说明.zh-CN.md`，`dist/蜂群体系的移植包/INSTALL.zh-CN.md`，`dist/蜂群体系的移植包/MANIFEST.json`，`dist/rebuild_swarm_pack.py`

---

## 2026-03-27 — 蜂群编队强约束时序（R4 拆为 R4a/R4b）

**升级特性**

- 主规则 **`.cursor/rules/hajimidog-4plus1-swarm.mdc`**：新增 **「蜂群编队启动与时序闸门」**——**R1+R2 关门 → R3**；**R4a** 与 R3 **并行**且仅产出**契约级**冻结验收表；**统帅**按表实测；**R4b** 在 **R3 关门 + 证据** 后做**事实核对**；工程类 P0 明确 **能跑通 + 约定生成物** 基线。
- **写码回合**最低触达由「R3+单一 R4」升级为 **R3 + R4a + R4b**；**满编**技术最小集为 **五段 Task**（R4 计两次）；**Task 首行锚点** `ROLE` 支持 **R4a|R4b**。
- 技能 **`12_蜂群规则与技能体系维护指南.md`**：误读表与 P0 文档审计项与新规对齐。

**好处举例**

1. **场景**：写码前 R4 先看到实现再写清单导致「自检放水」——R4a 与 R3 并行时**不得**纳入 R3 成果，R4b 后置对照证据核对。
2. **场景**：R3 未吃 R1/R2 就开工——闸门 **禁止** R1/R2 未关门即派发 R3 Task。

**关联路径**：`.cursor/rules/hajimidog-4plus1-swarm.mdc`，`.cursor/skills/hajimi-dog/03_AI行为准则/12_蜂群规则与技能体系维护指南.md`，`.cursor/swarm/RECENT-DECISIONS.md`

---

## 2026-03-27 — 军团：COND_PASS / 回声↔TC / 收敛熔断 + 异地同步长提示词落盘

**升级特性**

- **`.cursor/rules/hajimidog-legion.mdc`**：Phase Gate 与蜂群 **COND_PASS** 对齐（**COND_ID** 等留痕）；叶 OP Gate **PASS / 合法 COND_PASS** 放行、**FAIL** 不放行；回声 **PASS/BLOCK/COND_PASS** 与 **TC-ID / ACCEPT**、**R4a 表**互指；新增 **收敛与 5 轮熔断**（叶 OP 强 R1+R2 重入；军团强 R1 型+R2 型 Task 或改契约）。
- **技能 `20`**：§0.2a–0.2c、模板与 §3.3 P0、§4 与 **R3+R4a+R4b** 衔接。
- **异地升级**：新增可复制长提示词 **`.cursor/swarm/PROMPT-异地同步-蜂群与军团体系升级-2026-03-27.zh-CN.md`**（含人读摘要、整段粘贴块、路径清单、验收命令），便于在其他已装同源体系的环境中让 AI 按清单对齐。

**好处举例**

1. **场景**：另一台电脑只有旧 zip，不知道要改哪些文件——打开上述 **PROMPT** 整段丢给 Agent，可系统化 diff/覆盖/补节并跑 linter。
2. **场景**：军团回声与蜂群 TC 两套叙事——双向锚定与 **COND_PASS** 对齐减少「门里通过、门外又否」的误杀或误放。

**关联路径**：`.cursor/rules/hajimidog-legion.mdc`，`03_AI行为准则/20_军团模式回声校验与规模裁量.md`，`.cursor/swarm/PROMPT-异地同步-蜂群与军团体系升级-2026-03-27.zh-CN.md`，`.cursor/swarm/PROMPT-Claude环境-Agent升级-蜂群军团-2026-03-27.zh-CN.md`，`.cursor/swarm/RECENT-DECISIONS.md`（同日军团条目）

---

<!-- 新升级请复制以下骨架到「上方日期块之上」或紧接文末上一节之后，按时间倒序习惯可把新节写在「---」与第一个旧节之间 -->

<!--
## YYYY-MM-DD — 短标题

**升级特性**
- …

**好处举例**
1. **场景**：…
2. **场景**：…

**关联路径**：…
-->
