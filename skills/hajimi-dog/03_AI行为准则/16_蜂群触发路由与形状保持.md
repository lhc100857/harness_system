---
name: swarm-trigger-routing-and-shape
description: 当用户提到满编蜂群、蜂群满编、五段满编、军团架构满编/Cursor 总结/上下文压缩/Summarize 后续聊/CHAIN-STATE §A、**记忆接力/记接力/摘要后接力**、军团三级指挥、L3指挥链、蜂群、4+1、子代理、Task 真隔离、R1/R2/R3/R4、对抗质询、红队加压、用户身份与意图三字段、执行者身份、人设、产出用途、技能维护/完善 skills 要做几轮、对抗轮数、验收/P0/门禁、PASS·FAIL、形状保持、冻结事实、同一任务链续聊、别漂、别串台、落盘·RECENT-DECISIONS、工具成功但界面没变/静默成功/画布不更新、或要求提高蜂群触发/工作持久不变形时读取。纯单点领域查询（如硬件引脚）且用户明确与蜂群无关时可不读。与 `.cursor/rules/hajimidog-4plus1-swarm.mdc`「满编蜂群口令」「形状保持与防目标漂变」「用户身份与意图冻结」及 `hajimidog-legion.mdc`「术语分立」对齐。
---

# 蜂群触发路由与形状保持（提命中 · 控成本 · 不替代 `.mdc`）

**权威**：强制条款仍以 **`hajimidog-4plus1-swarm.mdc`** 为准；本文件是 **路由表 + 模板 + 极端情况备忘**，不得收窄写码回合 R3+R4、R2 强制、R4 实测等底线。

## 1. 分层触发（提高命中率 ≠ 提高 Task 成本）

| 层级 | 含义 | 统帅动作 |
|------|------|----------|
| **L0 总线** | 每条消息 | 解析→分发→汇流→续聊；**未必**开 Task |
| **L1 话术命中** | 用户用到下文「触发词」 | 首段可一句点明「按蜂群总线」；**仍按**最小充分角色集决定是否 Task |
| **L2 强制 Task** | 显式多角色、**满编蜂群**（五段口令）、写码回合、R2 命中等 | 必须走 Sub-agent（或诚实边界分节）；**满编军团**≠本条 |
| **L3 加码** | 高风险、集成碰壁 | 见 `.mdc` 高风险与集成专节 + 技能 `11` `13` |

**禁止**：用「看起来复杂」擅自 **满编蜂群**；用「提高触发率」把轻量追问一律打成五段。孤立「满编」须先澄清：蜂群五段 vs 军团架构满编（见 `.mdc`）。  
**备忘**：用户问 **「多少轮 / 技能对抗做多久」** **≠** L2 自动满编蜂群；先区分是 **运行时满编蜂群** 还是 **技能文档侧推演深度**，再按下表 Read **`02`** 对应章节。

**勿与「留痕三档」混淆**：本表 **L0–L3** 仅表示**触发与成本分层**；`.mdc` 中 **叙事稿 / 结构化全记录 / 字节级镜像** 是**留痕与审计**分层，二者**不得**互借代号。

## 2. 用户话术 → 建议 Read / 动作（非穷尽）

| 用户说法（示例） | 优先 Read / 动作 |
|------------------|------------------|
| 宿主业务开发、改代码/配置 | `00_全局索引` → 对应执行技能 |
| **满编蜂群** / 蜂群满编 / 五段满编 / 明示 R1·R2·R3·R4a·R4b；「四轮齐发」**且脚注=五段** | `.mdc` **满编蜂群口令** + `09`；启 **R1、R2、R3、R4a、R4b 各 ≥1 Task** |
| 军团架构满编 / 全编制军团 / 满编军团（仅架构） | `legion.mdc` **术语分立**；**不**触发蜂群五段 |
| 蜂群 / 4+1 / 子代理 / 别串台 | `.mdc` + `09`；若多视角分责则 **Task** |
| 验收 / P0 / 门禁 / 实测 / PASS | `.mdc` R4 节 + `12` §4 + `14` |
| 形状保持 / 冻结 / 别漂 / 接着上一轮 | **本文件** §4 + `.mdc` 形状保持 + `14` 落盘 |
| Summarize / 压缩上下文 / 长聊后怕丢条 | **`22` §12**（§A + 军令状节 Read）；**军团 Gate 放行前**同条 + `legion.mdc` Phase Gate；聊天摘要≠A 级 |
| **记忆接力** / **记接力** / **摘要后接力** / 外存接力 / 磁盘接力 | 蜂群 `.mdc` **「记忆接力」专节**（HANDOFF→§A→…）；**本轮首步 Read** 再派单；通用正文 `.cursor/swarm/记忆接力提示词.md`（`PORTING-PROMPT-MEMORY-RELAY.zh-CN.md` 为兼容入口）；与 **`22` §12.0** 互指 |
| **强制身份开场** / **身份三问** / **先三问** | `.mdc`「用户身份与意图冻结」口令升格 + **`12` §3.2** 模板与快速选项；**不**等于每条闲聊默认三问 |
| 领域不熟 / 对标 / 升格 | `11` |
| LLM·HTTP·集成·空 choices | `13` + `.mdc` 集成层 |
| 工具成功但界面没变 / 静默成功 / 画布不更新 / 日志 ✓ 但看不见 | **`17`**（调试清单）+ 必要时 `13`；仍按最小充分决定是否 **满编蜂群** |
| 改 `.mdc` / 规则冲突 / 维护技能 | `12` + 跑 `skills_linter.py` |
| 多少轮 / 要跑几轮 / 对抗完善技能选几轮 / 技能维护混沌做多久 | **`02`**「技能维护中的对抗深化：轮次与停损（小白向）」；**勿**把 **`05`** 的「50」当标准；运行时「满编蜂群」以 **`.mdc`** 口令为准 |
| 军团 / 军团模式 / 兵团 / Legion / L3 / 多部门 / 组织架构 | `18` + `19` + `20` + `.cursor/rules/hajimidog-legion.mdc`；激活军团模式全流程 |
| **军团三级指挥** / **L3 指挥链** / EX·MG·OP 链 / EX / MG / OP（军团）/ TIER=EX·MG·OP / 逐级指挥（军团语境）/ 分层指挥 / 中间层发包 / 下级 Task 发包 / 逐级主题汇流 / 逐级汇流块 / 不得越级（架构内） | **同上**（军团）；优先读 `legion.mdc` **术语分立**、**激活条件 §3**、**触发词表**；再 `18`→`19`→`20` |
| 含混单用「三级指挥」「真隔离三级」 | **先澄清** L3 指挥链 vs 蜂群 R1–R4；见 `legion.mdc` |
| 加角色 / 减角色 / 调整架构（军团上下文） | `18` §3.3；返回架构编辑 |
| 军令状 / 穿透 / Mission Brief | `19`；军令状机制 |
| 回声校验 / Echo Check | `20` §1；回声校验清单 |
| S/M/L / 规模判定 | `20` §2；规模裁量指南 |
| 外来 cursor_skills 包 / 外来领域包参考 | `15` + `external-cursor-pack-bridge/SKILL.md` |

## 3. 极端情况（统帅须显式处理，防「变形」）

1. **用户新一句与已冻结 A 级事实矛盾**：指出矛盾 → 请用户确认推翻项 → **重冻结**；禁止静默以新句为准。  
2. **用户从「闲聊」切回「继续改代码」**：一句恢复任务链指针（目标/待决/上次裁决）。  
3. **长对话多轮**：优先 **落盘或指针**；新 Task **条目化摘要**，禁默认「你懂的」。  
4. **「随便改改」但触及安全/持久化/规则语义**：按 **高风险** 或 **写码回合** 升格，勿当小改跳过 R3+R4。  
5. **子代理结论与仓库或终端冲突**：以 **仓库与命令输出** 为准，**重发 Task**（`.mdc` 已有，此处强调防漂）。  
6. **豁免口语模糊**（「别太正式」「随便说说」）：若无明示豁免关键词，**不**自动退出蜂群；若用户意图是豁免，可 **一行澄清** 是否适用退出/免 Task。  
7. **同会话多主题**：若用户在同一父会话中切换**明显不同**的冻结目标（如从方案文档跳到无关脚本），统帅应**新开父会话**或启用**不同 CHAIN** 并在 `.cursor/swarm/` 轮次表留痕，避免子代理目录「一桶杂烩」却假装同一形状保持。

## 4. 续聊任务状态（可复制模板 · 轮末建议保留）

```markdown
**蜂群任务状态**
- **任务链**：延续 / 新开（若新开，旧冻结条已声明作废：…）
- **身份与意图三字段**（各 A/B/C 或待决）：执行者身份（真实/假定）｜期望扮演身份（人设）｜产出下游用途
- **CHAIN / ROUND**：（若适用）CHAIN=… ROUND=… PARENT_REF=…
- **目标一句**：…
- **已拍板（A/B/C）**：…
- **待决**：…
- **转化落点**：R1/R2 →（→R3 / →R4 / 风险 / 待用户）…
- **验收**：PASS|COND_PASS|FAIL；未闭环 P0：…（可区分 TC-B- / TC-E-）
- **指针**：`.cursor/swarm/…` 或 `HANDOFF` / 无（不落盘原因一句）
```

## 5. 与技能 `15`（外部包）的关系

外来 **INDEX/触发词** 仅作 **方法论参考**；本仓库 **Task 底线与 R4 实测** 仍以 `.mdc` 为准，禁止用外来 workflow 顶替。

## 🔗 技能链条

- [热度权重: 7] `蜂群规则与技能体系维护指南`
- [热度权重: 6] `任务驱动型四角色对抗开发工作流`
- [热度权重: 5] `蜂群可验证结论与跨会话裁决落盘`
- [热度权重: 4] `领域不熟与需求升格蜂群协议`
- [热度权重: 5] `工具成功与UI不可见-调试清单`（`17`）
- [热度权重: 4] `军团模式组织架构动态生成`（`18`，L3 军团触发入口）

**规则锚点**：`.cursor/rules/hajimidog-4plus1-swarm.mdc` | `.cursor/rules/hajimidog-legion.mdc`（军团模式）
