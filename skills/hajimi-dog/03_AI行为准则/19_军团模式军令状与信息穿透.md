---
name: legion-mission-brief-penetration
description: 军团模式军令状（Mission Brief）的生成模板、穿透规则、上下文摘要契约与关键约束穿透标签。与 `.cursor/rules/hajimidog-legion.mdc` 对齐。
---

# 军团模式：军令状与信息穿透机制

**范围**：军团模式阶段四（军令状生成）的操作细则，以及全流程中信息传递的穿透纪律。

## 1. 军令状模板（可复制）

```markdown
# 军令状（Mission Brief）

**任务链 ID**：___
**生成时间**：___
**军团统帅**：主对话助手

---

## ① 原始需求（用户原话，禁止改写）

> [此处粘贴用户原始消息全文]

## ② 冻结目标句（统帅提炼，用户已确认）

[一句话目标]

## ③ 硬约束（不可违反）

- [PENETRATING] <安全/合规/数据隐私等关键约束>
- <技术限制>
- <工期/资源约束>
- …

## ④ 非目标（明确不做）

- <非目标 1>
- <非目标 2>
- <非目标 3>
- …

## ⑤ 成功标准（可验证）

- <标准 1：可测条件>
- <标准 2：可测条件>
- …

---

🔒 本文档为军令状，任何层级 Sub-agent 不得修改。发现矛盾须上报军团统帅。
```

## 2. 穿透规则详解

### 2.1 军令状传递纪律（按三层级细化）

| 传递路径 | 发送方 | 接收方 | 穿透规则 | 可追加内容 |
|---------|--------|--------|---------|-----------|
| 统帅 → EX | +1 | EX | 军令状**全文或落盘路径**（必须） | 战略指令区 |
| EX → MG（经统帅路由 **或** EX《下级 Task 发包》+ 统帅代发） | EX | MG | 军令状**全文或落盘路径**（必须） | 战略约束（标注追加者+层级=EX）；发包须满足 `hajimidog-legion.mdc` 中间层分发义务 |
| MG → OP（经统帅路由 **或** MG《下级 Task 发包》+ 统帅代发） | MG | OP | 军令状**全文或落盘路径**（必须） | 执行约束（标注追加者+层级=MG）；发包同上 |
| OP → 蜂群 R1–R4 | OP | L2 | **至少**携带军令状 ②③⑤ | 蜂群统帅按蜂群 `.mdc` 裁量 |
| 任何层级发现矛盾 | — | — | **上报**直接上级，最终达军团统帅 | — |

**注（与 `hajimidog-legion.mdc` 对齐）**：**L3 角色**（EX / MG / OP，含叶 OP）各对应一条军团架构上的 Task，穿透区须军令状**全文或落盘路径**。**R1–R4** 为 **L2** 蜂群内角色，**不**单独占用军团架构节点计数；由下表「OP→蜂群」行约束**至少 ②③⑤**，且 §2.5 **`[PENETRATING]`** 原文全链不变，常与叶 OP 穿透区中的路径 + Read 衔接。

### 2.2 EX 层穿透规则

- **可追加**：战略约束（技术路线偏好、跨域优先级排序等）
- **追加标注**（强制）：`追加者=<EX角色名> 追加层级=EX 追加轮次=ROUND-<N>`
- **追加效力**：**软约束**——下级应遵循，但与军令状硬约束冲突时以军令状为准
- **不得**：修改军令状五要素；替下级做实现决策

### 2.3 MG 层穿透规则

- **可追加**：执行约束（资源分配、时序依赖、工程规范等）
- **追加标注**（强制）：`追加者=<MG角色名> 追加层级=MG 追加轮次=ROUND-<N>`
- **追加效力**：**执行级约束**——OP 须遵循，但与军令状或 EX 战略约束冲突时以上级为准
- **不得**：修改军令状或 EX 追加的战略约束；跳过 OP 直接产出实现方案

### 2.4 冲突裁决序

军令状五要素（最高）> `[PENETRATING]` 标签 > EX 战略约束 > MG 执行约束 > OP 本层补充（最低）。冲突须**上报**而非自行裁决。

### 2.5 `[PENETRATING]` 标签

军令状 ③ 硬约束中标记了 `[PENETRATING]` 的条目：

- **传递时必须原文复制**，贯穿 EX → MG → OP → 蜂群全链路，不得摘要、改写或省略
- **优先级最高**：与任何层级追加信息冲突时，以 `[PENETRATING]` 为准
- **冲突上报**：任何层级发现冲突时须在向上汇流中**显式标明**冲突条目与层级
- **典型使用场景**：安全底线、法规合规、数据隐私、密钥策略、多租户隔离

### 2.6 禁止行为

- 禁止任何层级**删除**军令状中的条目
- 禁止将 EX/MG 的**推断**混入军令状正文（须放在追加区并标注追加者与层级）
- 禁止以「摘要」为名**替换**军令状原文（摘要仅用于上游摘要链，军令状本身不被摘要）
- 禁止假设下级「已经知道」军令状内容而不传递
- 禁止 MG 修改 EX 的追加内容（可追加新条目，不可改已有）
- 禁止追加内容**不标注**追加者与层级（无标注视为违规，Gate 时须拦截）

### 2.7 军令状冻结确认、修订与 G/L/O 标注（操作细则）

- **冻结前确认**：军令状 **②③④⑤** 定稿前须具备 **用户明示**（原话或可指针对象），或 **统帅草案 + 用户确认**（双留痕）；与 `.cursor/rules/hajimidog-legion.mdc` **军令状修订与穿透区正文的纪律**一致。**+1 禁止**在无上述确认时单独改写穿透区五要素或弱化 `[PENETRATING]`。
- **矛盾分流**：事实核对发现契约与证据不一致时，由统帅归类 **G（全局）/ L（局部）/ O（操作）**（定义与动作表见军团 `.mdc` **事实核对矛盾 · 多路升级**）；Sub-agent 仅输出**建议分类 + 证据等级**，**不得**自断全局。
- **EX/MG 追加区**：凡外部事实、厂商行为、可达性等 **R2 域**陈述，默认 **B/C** 或附 **待 R2**；**禁止**标 **A** 除非指回 **R2 Task 关门**或 **统帅可复核指针**。
- **`decisions_log`（推荐）**：军团层新增矛盾处置条目时建议带 `SCOPE=G|L|O`；若 `AFFECTS_SHAPE=yes` 而未获用户明示推翻与统帅留痕对齐形状，则 **Gate 不得假放行**（与军团 `.mdc` 同条）。

**可复制附言（贴入 `MISSION-BRIEF` 文末可选）**：

```markdown
## 冻结确认（示例）
- [ ] ②③④⑤ 已由用户明示确认 **或** 草案已获用户确认（指针：___）
- [ ] 已知矛盾分流将适用 G/L/O（见 legion `.mdc`）
```

## 3. 上下文摘要契约（详细说明）

### 3.1 三区结构

每层向下传递的 Task 正文包含三个语义区：

**穿透区**（不可改）：
- 军令状全文（首选）
- 或军令状落盘路径 + `请 Read 此路径获取军令状`

**本层追加区**（该层角色写）：
- **本层理解**：一句话说明本层角色如何理解自己要做的事
- **领域知识补充**：本层专业领域内对下级有帮助的信息
- **交付物定义**：明确期望下级产出什么、格式、粒度
- **时序或依赖说明**：下级产出是否依赖其他部门结果

**I/O 路由输入段**（Gate 通过后、统帅为下游 Task 组装时使用）：

统帅在「本层追加区」中追加以下结构，将上游产出路由给下游角色：

```
[I/O 路由输入]
依据 I/O 契约表，你的输入如下：

| 来源角色 | 产出名称 | 内容（摘要或落盘路径） |
|---------|---------|-------------------|
| 角色A | 产出X | （粘贴关键内容或路径） |
| 角色B | 产出Y | （粘贴关键内容或路径） |

你的输出须符合 I/O 契约表中的「输出格式规范」：<此处复制该角色的格式规范>
你的输出须包含 decisions_log（已决策 + 排除项 + 理由）。
```

**I/O 路由规则**：
- 内容短（<500 字）→ 直接粘贴到 Task 正文
- 内容长 → 提供落盘路径 + 关键段落摘要
- **禁止**将上游全部原始产出不裁剪地一锅端
- **禁止**将 I/O 路由段混入穿透区

**禁区**（强制遵守）：
- 不得引用**同层级他部门**的结论，除非军团统帅已按 I/O 契约裁剪转发
- 不得将**推断**标为「已确认」
- 不得把本层理解**混入**穿透区军令状正文

### 3.2 为何这样设计

| 区域 | 解决的问题 |
|------|-----------|
| 穿透区 | 防止原始需求在多层传递中**丢失** |
| 本层追加区 | 允许每层加入专业知识（分层的**价值所在**） |
| 禁区 | 防止中间层的理解**污染**原始约束 |

### 3.3 逐级主题汇流块（向上汇报）

各 L3 角色向上级（及 OP 向 MG/统帅）交付时，须含 **《逐级主题汇流块》** 表格字段：本层职责一句、直连下级清单与状态、下级到齐状态、主题摘要、矛盾与风险、上送指针。字段语义与 OP/蜂群收口例外见 **`.cursor/rules/hajimidog-legion.mdc`** 专节。推荐可另落盘 `legion/rollups/ROLLUP-<CHAIN>-<TIER>-<slug>.md`。

### 3.4 小组信息空间（与穿透并列、不替代）

**领导辖内**执行期共识区：路径与边界见 **`.cursor/rules/hajimidog-legion.mdc`「小组信息空间」** 与 **`legion/group-space/README.md`**。组空间**不得**升格为穿透区或替代军令状；跨组引用须经统帅 I/O 裁剪。

## 4. 落盘规范

| 文件 | 路径 | 写入时机 |
|------|------|---------|
| 军令状 | `.cursor/swarm/legion/MISSION-BRIEF.md` | 阶段四架构冻结时 |
| 架构图 | `.cursor/swarm/legion/ARCHITECTURE.md` | 同上 |
| 回声校验记录 | `.cursor/swarm/legion/ECHO-CHECK.md` | 阶段六汇流校验后 |
| 逐级汇流（可选） | `.cursor/swarm/legion/rollups/ROLLUP-*.md` | 各角色完成直连汇流后 |
| 小组信息空间（可选） | `.cursor/swarm/legion/group-space/<CHAIN>/W<波次>/<TIER>-<slug>/CONSENSUS.md` | 执行窗内；波次收口只读/归档 |

**命名规则**：若同会话有多条军团任务链，追加 `CHAIN-ID` 后缀区分。

**与蜂群落盘关系**：军团落盘是 L3 层增量；蜂群 `.mdc` 要求的 `RECENT-DECISIONS.md` 等仍照常。

## 5. 常见误读

| 误读 | 正读 |
|------|------|
| 「军令状太长，下级不看」 | 军令状是**穿透文档**，每层 Task **必须**携带；不读不是不传 |
| 「中间层可以替下级做决定」 | 中间层可追加知识，但**不得**替代军令状的硬约束 |
| 「蜂群 R1–R4 也要看完整军令状」 | 蜂群 R1–R4 **至少**看 ②③⑤；完整军令状由蜂群统帅裁量 |
| 「穿透 = 所有信息都传下去」 | 穿透仅指军令状五要素；其他信息仍按最小充分原则裁剪 |
| 「只有统帅能下发 Task」 | **带下级 L3 角色**（含架构许可的非叶 OP）可起草**直接下级**《下级 Task 发包》；平台限制时由 **统帅代发**（见 `hajimidog-legion.mdc`） |
| 「组空间 = 全局事实」 | 组空间仅**组内执行期**有效；**不得**替代 ROLLUP 必填项与军令状 |
| 「`hajimidog-legion.mdc` 说每个 Sub-agent 都要军令状全文」 | **L3 架构角色 Task**须全文或路径；**R1–R4** 按本章 **至少 ②③⑤** 与 §2.5 **`[PENETRATING]`**，并遵蜂群 `.mdc`；二者**分层衔接**，不是两套互相取消的义务 |
| 「+1 可以顺手改军令状省事」 | 穿透区修订须 **用户明示** 或 **草案+用户确认**；见 §2.7 与军团 `.mdc` |
| 「EX/MG 写『已核实 A』就算 R2」 | **R2 型 A** 仅 R2 关门或统帅可复核指针；EX/MG 默认 B/C |

## 🔗 技能链条

- [热度权重: 9] `军团模式组织架构动态生成`（`18`）
- [热度权重: 9] `军团模式回声校验与规模裁量`（`20`）
- [热度权重: 6] `蜂群可验证结论与跨会话裁决落盘`（`14`，A/B/C 分级对齐）
- [热度权重: 5] `蜂群规则与技能体系维护指南`（`12`，模板与维护纪律）

**规则锚点**：`.cursor/rules/hajimidog-legion.mdc`
